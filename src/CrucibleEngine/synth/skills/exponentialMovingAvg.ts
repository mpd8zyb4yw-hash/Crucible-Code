// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — exponentialMovingAverage produces the EMA series of a numeric array given a smoothing alpha, seeding the first value with arr[0].\nexport function exponentialMovingAverage(arr: number[], alpha: number): number[] {\n  if (arr.length === 0) return [];\n  const out: number[] = [arr[0]];\n  for (let i = 1; i < arr.length; i++) {\n    out.push(alpha * arr[i] + (1 - alpha) * out[i - 1]);\n  }\n  return out;\n}\n\n"

registerSkill({
  id: "exponential-moving-average",
  summary: "exponentialMovingAverage produces the EMA series of a numeric array given a smoothing alpha, seeding the first value with arr[0].",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bexponentialMovingAverage\\b", 'i'))) sc += 0.5
  if (s.has(new RegExp("EMA series", 'i'))) sc += 0.2
  if (s.has(new RegExp("smoothing alpha", 'i'))) sc += 0.15
  if (s.has(new RegExp("numeric array", 'i'))) sc += 0.1
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/exponentialMovingAvg.ts", content: IMPL }]
  },
})
