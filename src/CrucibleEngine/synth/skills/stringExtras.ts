// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — pluralize, stripHtml, initials, words, nl2br, repeat, padLines.\nexport function pluralize(word: string, count: number, plural?: string): string {\n  if (count === 1) return word\n  return plural ?? (word.endsWith('s') || word.endsWith('x') || word.endsWith('z') ||\n    word.endsWith('ch') || word.endsWith('sh') ? word + 'es' : word + 's')\n}\n\nexport function stripHtml(html: string): string {\n  return html.replace(/<[^>]*>/g, '').replace(/&amp;/g, '&').replace(/&lt;/g, '<')\n    .replace(/&gt;/g, '>').replace(/&quot;/g, '\"').replace(/&#39;/g, \"'\").trim()\n}\n\nexport function initials(name: string): string {\n  return name.trim().split(/\\s+/).map(w => w[0]?.toUpperCase() ?? '').join('')\n}\n\nexport function words(str: string): string[] {\n  return str.trim().split(/[^a-zA-Z0-9']+/).filter(Boolean)\n}\n\nexport function nl2br(str: string): string {\n  return str.replace(/\\r?\\n/g, '<br>')\n}\n"

registerSkill({
  id: "string-extras",
  summary: "pluralize, stripHtml, initials, words, nl2br, repeat, padLines.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bpluralize\\b", 'i'))) sc += 0.65
  if (s.has(new RegExp("\\bstrip.*html|remove.*html.*tag", 'i'))) sc += 0.6
  if (s.has(new RegExp("\\binitials\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("\\bpluralize.*stripHtml|html.*plural", 'i'))) sc += 0.5
  if (s.has(new RegExp("\\bnl2br\\b|\\bnewline.*br|line.*break.*html", 'i'))) sc += 0.55
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/stringExtras.ts", content: IMPL }]
  },
})
