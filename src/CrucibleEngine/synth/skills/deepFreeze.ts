// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — Recursively freeze an object so it cannot be mutated.\nexport function deepFreeze<T>(obj: T): Readonly<T> {\n  if (obj === null || typeof obj !== 'object') return obj\n  Object.freeze(obj)\n  for (const key of Object.keys(obj as object)) {\n    deepFreeze((obj as Record<string, unknown>)[key])\n  }\n  return obj as Readonly<T>\n}\n"

registerSkill({
  id: "deep-freeze",
  summary: "Recursively freeze an object so it cannot be mutated.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bdeep.*freeze|freeze.*deep", 'i'))) sc += 0.7
  if (s.has(new RegExp("\\bdeepFreeze\\b", 'i'))) sc += 0.4
  if (s.has(new RegExp("\\bimmutable.*object|freeze.*nested", 'i'))) sc += 0.4
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/deepFreeze.ts", content: IMPL }]
  },
})
