// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — abbreviateNumber abbreviates large numbers to 1.2K, 1.5M, or 1B format with one decimal trimmed if trailing zero.\nexport function abbreviateNumber(n: number): string {\n  const abs = Math.abs(n), sign = n < 0 ? '-' : ''\n  const fmt = (v: number, s: string) => sign + (Math.round(v*10)/10).toString().replace(/\\.0$/, '') + s\n  if (abs >= 1e9) return fmt(abs/1e9, 'B')\n  if (abs >= 1e6) return fmt(abs/1e6, 'M')\n  if (abs >= 1e3) return fmt(abs/1e3, 'K')\n  return String(n)\n}\n"

registerSkill({
  id: "abbreviate-number",
  summary: "abbreviateNumber abbreviates large numbers to 1.2K, 1.5M, or 1B format with one decimal trimmed if trailing zero.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\babbreviateNumber\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("abbreviat.*number|compact.*number", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/abbreviateNumber.ts", content: IMPL }]
  },
})
