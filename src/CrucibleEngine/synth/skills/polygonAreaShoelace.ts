// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — Compute the absolute area of a polygon via the shoelace formula using polygonAreaShoelace; fewer than 3 points yields 0.\nexport function polygonAreaShoelace(points: [number, number][]): number {\n  const n = points.length;\n  if (n < 3) return 0;\n  let sum = 0;\n  for (let i = 0; i < n; i++) {\n    const [x1, y1] = points[i];\n    const [x2, y2] = points[(i + 1) % n];\n    sum += x1 * y2 - x2 * y1;\n  }\n  return Math.abs(sum) / 2;\n}\n\n"

registerSkill({
  id: "polygon-area-shoelace",
  summary: "Compute the absolute area of a polygon via the shoelace formula using polygonAreaShoelace; fewer than 3 points yields 0.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bpolygonAreaShoelace\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("shoelace", 'i'))) sc += 0.2
  if (s.has(new RegExp("\\barea\\b", 'i'))) sc += 0.15
  if (s.has(new RegExp("\\bpolygon\\b", 'i'))) sc += 0.1
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/polygonAreaShoelace.ts", content: IMPL }]
  },
})
