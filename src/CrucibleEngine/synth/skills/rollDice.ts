// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — rollDice returns the sum of rolling count dice each with the given number of sides, using an injected rng.\nexport function rollDice(sides, count, rng) {\n  let total = 0\n  for (let i = 0; i < count; i++) total += Math.floor(rng() * sides) + 1\n  return total\n}\n"

registerSkill({
  id: "roll-dice",
  summary: "rollDice returns the sum of rolling count dice each with the given number of sides, using an injected rng.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\brollDice\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("roll.*dice|dice.*roll", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/rollDice.ts", content: IMPL }]
  },
})
