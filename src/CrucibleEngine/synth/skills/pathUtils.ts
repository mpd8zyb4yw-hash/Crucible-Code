// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — joinPath, getExtension, getFileName, getDirName, stripExtension, normalizePath.\nexport function joinPath(...parts: string[]): string {\n  return parts.join('/').replace(/\\/+/g, '/').replace(/\\/$/, '') || '/'\n}\n\nexport function getExtension(filePath: string): string {\n  const base = filePath.split('/').pop() ?? ''\n  const dot = base.lastIndexOf('.')\n  return dot <= 0 ? '' : base.slice(dot)\n}\n\nexport function getFileName(filePath: string): string {\n  return filePath.split('/').pop() ?? ''\n}\n\nexport function getDirName(filePath: string): string {\n  const parts = filePath.split('/')\n  parts.pop()\n  return parts.join('/') || '/'\n}\n\nexport function stripExtension(filePath: string): string {\n  const ext = getExtension(filePath)\n  return ext ? filePath.slice(0, -ext.length) : filePath\n}\n\nexport function normalizePath(filePath: string): string {\n  const parts = filePath.split('/')\n  const out: string[] = []\n  for (const p of parts) {\n    if (p === '..') out.pop()\n    else if (p !== '.') out.push(p)\n  }\n  return out.join('/') || '/'\n}\n"

registerSkill({
  id: "path-utils",
  summary: "joinPath, getExtension, getFileName, getDirName, stripExtension, normalizePath.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bjoinPath\\b|\\bjoin.*path\\b", 'i'))) sc += 0.55
  if (s.has(new RegExp("\\bgetExtension\\b|\\bfile.*extension", 'i'))) sc += 0.5
  if (s.has(new RegExp("\\bgetFileName\\b|\\bfile.*name", 'i'))) sc += 0.45
  if (s.has(new RegExp("\\bpath.*util|normalizePath\\b", 'i'))) sc += 0.45
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/pathUtils.ts", content: IMPL }]
  },
})
