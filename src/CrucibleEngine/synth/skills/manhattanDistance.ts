// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — Compute the L1 Manhattan distance between two 2D points using manhattanDistance.\nexport function manhattanDistance(ax: number, ay: number, bx: number, by: number): number {\n  return Math.abs(bx - ax) + Math.abs(by - ay);\n}\n\n"

registerSkill({
  id: "manhattan-distance-l1",
  summary: "Compute the L1 Manhattan distance between two 2D points using manhattanDistance.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bmanhattanDistance\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("[Mm]anhattan", 'i'))) sc += 0.2
  if (s.has(new RegExp("\\bL1\\b", 'i'))) sc += 0.15
  if (s.has(new RegExp("\\bdistance\\b", 'i'))) sc += 0.1
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/manhattanDistance.ts", content: IMPL }]
  },
})
