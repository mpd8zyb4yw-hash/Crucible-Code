// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — businessDaysBetween counts Mon-Fri days strictly between date a and inclusive of date b (a <= b), in UTC.\nexport function businessDaysBetween(a: Date, b: Date): number {\n  let count = 0\n  const cur = new Date(Date.UTC(a.getUTCFullYear(), a.getUTCMonth(), a.getUTCDate() + 1))\n  const end = new Date(Date.UTC(b.getUTCFullYear(), b.getUTCMonth(), b.getUTCDate()))\n  while (cur <= end) {\n    const dow = cur.getUTCDay()\n    if (dow !== 0 && dow !== 6) count++\n    cur.setUTCDate(cur.getUTCDate() + 1)\n  }\n  return count\n}\n"

registerSkill({
  id: "business-days-between",
  summary: "businessDaysBetween counts Mon-Fri days strictly between date a and inclusive of date b (a <= b), in UTC.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bbusinessDaysBetween\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("business days|working days", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/businessDaysBetween.ts", content: IMPL }]
  },
})
