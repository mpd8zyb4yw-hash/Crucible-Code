// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — Finite state machine with typed states, transitions, guards, and actions.\nexport interface Transition<S extends string, E extends string> {\n  from: S | '*'\n  event: E\n  to: S\n  guard?: () => boolean\n  action?: () => void\n}\n\nexport class StateMachine<S extends string, E extends string> {\n  private state: S\n  private transitions: Transition<S, E>[]\n  private history: S[] = []\n\n  constructor(initial: S, transitions: Transition<S, E>[]) {\n    this.state = initial\n    this.transitions = transitions\n    this.history = [initial]\n  }\n\n  getState(): S { return this.state }\n  getHistory(): S[] { return [...this.history] }\n\n  send(event: E): boolean {\n    const t = this.transitions.find(t =>\n      (t.from === '*' || t.from === this.state) &&\n      t.event === event &&\n      (!t.guard || t.guard())\n    )\n    if (!t) return false\n    t.action?.()\n    this.state = t.to\n    this.history.push(t.to)\n    return true\n  }\n\n  can(event: E): boolean {\n    return this.transitions.some(t =>\n      (t.from === '*' || t.from === this.state) &&\n      t.event === event &&\n      (!t.guard || t.guard())\n    )\n  }\n}\n"

registerSkill({
  id: "state-machine",
  summary: "Finite state machine with typed states, transitions, guards, and actions.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bstate.*machine|finite.*state", 'i'))) sc += 0.7
  if (s.has(new RegExp("\\bStateMachine\\b", 'i'))) sc += 0.4
  if (s.has(new RegExp("\\bfsm\\b|\\btransition.*state|state.*transition", 'i'))) sc += 0.55
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/stateMachine.ts", content: IMPL }]
  },
})
