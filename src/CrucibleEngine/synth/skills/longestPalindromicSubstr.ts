// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — longestPalindromicSubstr returns the first longest palindromic substring of a string.\nexport function longestPalindromicSubstr(s: string): string {\n  if (s.length < 2) return s\n  let start = 0, maxLen = 1\n  const expand = (l: number, r: number): [number, number] => {\n    while (l >= 0 && r < s.length && s[l] === s[r]) { l--; r++ }\n    return [l + 1, r - l - 1]\n  }\n  for (let i = 0; i < s.length; i++) {\n    const [s1, l1] = expand(i, i)\n    const [s2, l2] = expand(i, i + 1)\n    if (l1 > maxLen) { maxLen = l1; start = s1 }\n    if (l2 > maxLen) { maxLen = l2; start = s2 }\n  }\n  return s.slice(start, start + maxLen)\n}\n"

registerSkill({
  id: "longest-palindromic-substr",
  summary: "longestPalindromicSubstr returns the first longest palindromic substring of a string.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\blongestPalindromicSubstr\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("longest palindrom", 'i'))) sc += 0.35
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/longestPalindromicSubstr.ts", content: IMPL }]
  },
})
