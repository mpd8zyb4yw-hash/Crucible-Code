// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — longestIncreasingSubsequence returns the length of the longest strictly increasing subsequence of a number array.\nexport function longestIncreasingSubsequence(arr: number[]): number {\n  const tails: number[] = []\n  for (const x of arr) {\n    let lo = 0, hi = tails.length\n    while (lo < hi) { const mid = (lo + hi) >> 1; tails[mid] < x ? lo = mid + 1 : hi = mid }\n    tails[lo] = x\n  }\n  return tails.length\n}\n"

registerSkill({
  id: "longest-increasing-subsequence",
  summary: "longestIncreasingSubsequence returns the length of the longest strictly increasing subsequence of a number array.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\blongestIncreasingSubsequence\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("longest increasing subsequence|\\blis\\b", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/longestIncreasingSubsequence.ts", content: IMPL }]
  },
})
