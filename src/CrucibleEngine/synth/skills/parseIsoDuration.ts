// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — parseIsoDuration converts an ISO-8601 duration like PT1H30M or P1DT2H to milliseconds; invalid input returns NaN.\nexport function parseIsoDuration(s: string): number {\n  const m = s.match(/^P(?:(\\d+)Y)?(?:(\\d+)M)?(?:(\\d+)D)?(?:T(?:(\\d+)H)?(?:(\\d+)M)?(?:(\\d+(?:\\.\\d+)?)S)?)?$/)\n  if (!m) return NaN\n  const [,Y,Mo,D,H,Mi,S] = m\n  return ((+(Y??0)*365 + +(Mo??0)*30 + +(D??0))*24*3600 + +(H??0)*3600 + +(Mi??0)*60 + +(S??0)) * 1000\n}\n"

registerSkill({
  id: "parse-iso-duration",
  summary: "parseIsoDuration converts an ISO-8601 duration like PT1H30M or P1DT2H to milliseconds; invalid input returns NaN.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bparseIsoDuration\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("iso.*duration|duration.*iso.*8601", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/parseIsoDuration.ts", content: IMPL }]
  },
})
