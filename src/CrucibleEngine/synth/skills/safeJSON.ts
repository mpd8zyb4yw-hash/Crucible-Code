// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — Safe JSON parse (returns null on error) and safe stringify (handles circular).\nexport function safeJSONParse<T = unknown>(str: string, fallback: T | null = null): T | null {\n  try { return JSON.parse(str) } catch { return fallback }\n}\n\nexport function safeJSONStringify(value: unknown, space?: number): string {\n  const seen = new WeakSet()\n  return JSON.stringify(value, (_, v) => {\n    if (typeof v === 'object' && v !== null) {\n      if (seen.has(v)) return '[Circular]'\n      seen.add(v)\n    }\n    return v\n  }, space)\n}\n"

registerSkill({
  id: "safe-json",
  summary: "Safe JSON parse (returns null on error) and safe stringify (handles circular).",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bsafe.*json.*parse|json.*parse.*safe", 'i'))) sc += 0.65
  if (s.has(new RegExp("\\bsafeJSON\\b|\\bsafeJSONParse\\b", 'i'))) sc += 0.4
  if (s.has(new RegExp("\\bjson.*parse.*error|parse.*json.*try.*catch", 'i'))) sc += 0.45
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/safeJSON.ts", content: IMPL }]
  },
})
