// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — Format a number with thousand separators and optional decimal places.\nexport function formatNumber(\n  value: number,\n  decimals?: number,\n  decimalSep = '.',\n  thousandSep = ',',\n): string {\n  const fixed = decimals !== undefined ? value.toFixed(decimals) : String(value)\n  const [int, dec] = fixed.split('.')\n  const intFormatted = int.replace(/\\B(?=(\\d{3})+(?!\\d))/g, thousandSep)\n  return dec !== undefined ? `${intFormatted}${decimalSep}${dec}` : intFormatted\n}\n"

registerSkill({
  id: "format-number",
  summary: "Format a number with thousand separators and optional decimal places.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bformat.*number|number.*format", 'i'))) sc += 0.55
  if (s.has(new RegExp("\\bformatNumber\\b", 'i'))) sc += 0.3
  if (s.has(new RegExp("\\bthousand.*separator|comma.*thousand", 'i'))) sc += 0.5
  if (s.has(new RegExp("\\bnumber.*comma|comma.*separat", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/formatNumber.ts", content: IMPL }]
  },
})
