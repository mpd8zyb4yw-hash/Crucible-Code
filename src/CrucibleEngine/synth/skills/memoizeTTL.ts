// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — Memoize with per-entry TTL expiration and optional max size.\nexport function memoizeTTL<T extends unknown[], R>(\n  fn: (...args: T) => R,\n  ttlMs: number,\n  now: () => number = Date.now,\n): (...args: T) => R {\n  const cache = new Map<string, { value: R; exp: number }>()\n  return (...args: T): R => {\n    const key = JSON.stringify(args)\n    const entry = cache.get(key)\n    if (entry && now() < entry.exp) return entry.value\n    const value = fn(...args)\n    cache.set(key, { value, exp: now() + ttlMs })\n    return value\n  }\n}\n"

registerSkill({
  id: "memoize-ttl",
  summary: "Memoize with per-entry TTL expiration and optional max size.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bmemoize.*ttl|ttl.*memoize", 'i'))) sc += 0.7
  if (s.has(new RegExp("\\bcache.*expire|expir.*cache.*key", 'i'))) sc += 0.55
  if (s.has(new RegExp("\\bmemoizeTTL\\b", 'i'))) sc += 0.4
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/memoizeTTL.ts", content: IMPL }]
  },
})
