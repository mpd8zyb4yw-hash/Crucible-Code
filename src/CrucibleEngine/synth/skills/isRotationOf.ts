// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — isRotationOf reports whether one string is a rotation of another.\nexport function isRotationOf(a: string, b: string): boolean {\n  return a.length === b.length && (a + a).includes(b)\n}\n"

registerSkill({
  id: "is-rotation-of",
  summary: "isRotationOf reports whether one string is a rotation of another.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bisRotationOf\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("string rotation|is.*rotation", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/isRotationOf.ts", content: IMPL }]
  },
})
