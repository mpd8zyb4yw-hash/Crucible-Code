// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — longestCommonSubsequence returns the length of the longest common subsequence of two strings.\nexport function longestCommonSubsequence(a: string, b: string): number {\n  const m = a.length, n = b.length\n  const dp: number[][] = Array.from({ length: m + 1 }, () => new Array(n + 1).fill(0))\n  for (let i = 1; i <= m; i++)\n    for (let j = 1; j <= n; j++)\n      dp[i][j] = a[i - 1] === b[j - 1] ? dp[i - 1][j - 1] + 1 : Math.max(dp[i - 1][j], dp[i][j - 1])\n  return dp[m][n]\n}\n"

registerSkill({
  id: "longest-common-subsequence",
  summary: "longestCommonSubsequence returns the length of the longest common subsequence of two strings.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\blongestCommonSubsequence\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("longest common subsequence|\\blcs\\b", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/longestCommonSubsequence.ts", content: IMPL }]
  },
})
