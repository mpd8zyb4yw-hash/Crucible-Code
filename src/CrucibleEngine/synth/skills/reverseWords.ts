// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — reverseWords reverses the order of words in a string, collapsing repeated whitespace and trimming.\nexport function reverseWords(s: string): string {\n  return s.trim().split(/\\s+/).filter(Boolean).reverse().join(' ')\n}\n"

registerSkill({
  id: "reverse-words",
  summary: "reverseWords reverses the order of words in a string, collapsing repeated whitespace and trimming.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\breverseWords\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("reverse.*word|word.*order.*reverse", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/reverseWords.ts", content: IMPL }]
  },
})
