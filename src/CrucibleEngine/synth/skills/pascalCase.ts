// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — Convert a string to PascalCase.\nexport function pascalCase(str: string): string {\n  return str\n    .replace(/[-_\\s]+(.)/g, (_, c) => c.toUpperCase())\n    .replace(/^[a-z]/, c => c.toUpperCase())\n}\n"

registerSkill({
  id: "pascal-case",
  summary: "Convert a string to PascalCase.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bpascal[- ]?case\\b", 'i'))) sc += 0.7
  if (s.has(new RegExp("\\bpascalCase\\b", 'i'))) sc += 0.3
  if (s.has(new RegExp("\\bupper[- ]?camel\\b", 'i'))) sc += 0.4
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/pascalCase.ts", content: IMPL }]
  },
})
