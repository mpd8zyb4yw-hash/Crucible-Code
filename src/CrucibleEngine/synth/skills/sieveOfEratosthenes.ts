// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — Sieve of Eratosthenes returning all prime numbers less than or equal to n via sieveOfEratosthenes.\nexport function sieveOfEratosthenes(n: number): number[] {\n  if (n < 2) return [];\n  const sieve: boolean[] = new Array(n + 1).fill(true);\n  sieve[0] = false;\n  sieve[1] = false;\n  for (let i = 2; i * i <= n; i++) {\n    if (sieve[i]) {\n      for (let j = i * i; j <= n; j += i) sieve[j] = false;\n    }\n  }\n  const primes: number[] = [];\n  for (let i = 2; i <= n; i++) if (sieve[i]) primes.push(i);\n  return primes;\n}\n\n"

registerSkill({
  id: "sieve-of-eratosthenes",
  summary: "Sieve of Eratosthenes returning all prime numbers less than or equal to n via sieveOfEratosthenes.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bsieveOfEratosthenes\\b", 'i'))) sc += 0.5
  if (s.has(new RegExp("\\bprime\\b", 'i'))) sc += 0.2
  if (s.has(new RegExp("\\bsieve\\b", 'i'))) sc += 0.2
  if (s.has(new RegExp("\\bEratosthenes\\b", 'i'))) sc += 0.2
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/sieveOfEratosthenes.ts", content: IMPL }]
  },
})
