// Generated by generate.ts — hidden adversarial suite for transitive-closure.
// Run via `npx tsx __audit__/transitiveClosure.hidden.ts` inside the scratch project.
import { transitiveClosure } from '../src/transitiveClosure'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("chain", transitiveClosure(3,[[0,1],[1,2]]), [[true,true,true],[false,true,true],[false,false,true]])
await check("self always true", transitiveClosure(2,[])[0][0], true)
await check("no edges", transitiveClosure(2,[]), [[true,false],[false,true]])
await check("direct only", transitiveClosure(2,[[0,1]]), [[true,true],[false,true]])
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
