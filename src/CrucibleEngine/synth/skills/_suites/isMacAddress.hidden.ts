// Generated by generate.ts — hidden adversarial suite for is-mac-address.
// Run via `npx tsx __audit__/isMacAddress.hidden.ts` inside the scratch project.
import { isMacAddress } from '../src/isMacAddress'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("valid colon-separated MAC", isMacAddress("01:23:45:67:89:ab"), true)
await check("valid hyphen-separated uppercase MAC", isMacAddress("01-23-45-67-89-AB"), true)
await check("mixed separators rejected", isMacAddress("01:23-45:67:89:ab"), false)
await check("too few octets rejected", isMacAddress("01:23:45:67:89"), false)
await check("empty string rejected", isMacAddress(""), false)
await check("all-FF broadcast MAC valid", isMacAddress("AA:BB:CC:DD:EE:FF"), true)
await check("non-hex character rejected", isMacAddress("0g:23:45:67:89:ab"), false)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
