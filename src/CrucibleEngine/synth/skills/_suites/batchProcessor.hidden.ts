// Generated by generate.ts — hidden adversarial suite for batch-processor.
// Run via `npx tsx __audit__/batchProcessor.hidden.ts` inside the scratch project.
import { processBatches } from '../src/batchProcessor'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("processes all", processBatches([1,2,3,4,5], async b=>b.map(x=>x*2), {batchSize:2}), [2,4,6,8,10])
await check("empty input", processBatches([], async b=>b), [])
await check("single batch", processBatches([1,2,3], async b=>b, {batchSize:10}), [1,2,3])
await check("onBatch called", (async()=>{let calls=0;await processBatches([1,2,3,4],async b=>b,{batchSize:2,onBatch:()=>calls++});return calls})(), 2)
await check("concurrency 2", processBatches([1,2,3,4,5,6], async b=>b.map(x=>x+10), {batchSize:2,concurrency:2}), [11,12,13,14,15,16])
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
