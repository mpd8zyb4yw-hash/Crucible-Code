// Generated by generate.ts — hidden adversarial suite for strip-ansi-codes.
// Run via `npx tsx __audit__/stripAnsiCodes.hidden.ts` inside the scratch project.
import { stripAnsiCodes } from '../src/stripAnsiCodes'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("red color", stripAnsiCodes("\x1B[31mhello\x1B[0m"), "hello")
await check("bold", stripAnsiCodes("\x1B[1mtext\x1B[0m"), "text")
await check("no codes", stripAnsiCodes("plain"), "plain")
await check("empty", stripAnsiCodes(""), "")
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
