// Generated by generate.ts — hidden adversarial suite for map-values.
// Run via `npx tsx __audit__/mapValues.hidden.ts` inside the scratch project.
import { mapValues, mapKeys, filterValues } from '../src/mapValues'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("mapValues double", mapValues({a:1,b:2,c:3}, v=>v*2), {"a":2,"b":4,"c":6})
await check("mapValues empty", mapValues({}, v=>v), {})
await check("mapKeys uppercase", mapKeys({a:1,b:2}, k=>k.toUpperCase()), {"A":1,"B":2})
await check("filterValues positive", filterValues({a:1,b:-1,c:2}, v=>v>0), {"a":1,"c":2})
await check("filterValues empty result", filterValues({a:1}, v=>v>10), {})
await check("filterValues with key", filterValues({aa:1,b:2,cc:3}, (_,k)=>k.length>1), {"aa":1,"cc":3})
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
