// Generated by generate.ts — hidden adversarial suite for quarter-of.
// Run via `npx tsx __audit__/quarterOf.hidden.ts` inside the scratch project.
import { quarterOf } from '../src/quarterOf'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("Q1 Jan", quarterOf(new Date("2024-01-15T00:00:00Z")), 1)
await check("Q1 Mar", quarterOf(new Date("2024-03-31T00:00:00Z")), 1)
await check("Q2 Apr", quarterOf(new Date("2024-04-01T00:00:00Z")), 2)
await check("Q3 Aug", quarterOf(new Date("2024-08-01T00:00:00Z")), 3)
await check("Q4 Dec", quarterOf(new Date("2024-12-31T00:00:00Z")), 4)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
