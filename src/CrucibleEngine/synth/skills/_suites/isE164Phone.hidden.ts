// Generated by generate.ts — hidden adversarial suite for is-e164-phone.
// Run via `npx tsx __audit__/isE164Phone.hidden.ts` inside the scratch project.
import { isE164Phone } from '../src/isE164Phone'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("valid US E.164 number", isE164Phone("+14155552671"), true)
await check("minimal single-digit number", isE164Phone("+1"), true)
await check("leading zero after plus rejected", isE164Phone("+0123"), false)
await check("missing plus rejected", isE164Phone("14155552671"), false)
await check("plus only rejected", isE164Phone("+"), false)
await check("16 digits exceeds max", isE164Phone("+1234567890123456"), false)
await check("exactly 15 digits allowed", isE164Phone("+123456789012345"), true)
await check("spaces rejected", isE164Phone("+44 20"), false)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
