// Generated by generate.ts — hidden adversarial suite for random-string-from.
// Run via `npx tsx __audit__/randomStringFrom.hidden.ts` inside the scratch project.
import { randomStringFrom } from '../src/randomStringFrom'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("correct length", randomStringFrom(5,"abc",()=>0).length, 5)
await check("uses alphabet", randomStringFrom(3,"a",()=>0), "aaa")
await check("zero length", randomStringFrom(0,"abc",()=>0), "")
await check("picks index", randomStringFrom(1,"xyz",()=>0.5), "y")
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
