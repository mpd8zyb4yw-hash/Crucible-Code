// Generated by generate.ts — hidden adversarial suite for count-leading-zeros-32.
// Run via `npx tsx __audit__/countLeadingZeros32.hidden.ts` inside the scratch project.
import { countLeadingZeros32 } from '../src/countLeadingZeros32'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("one", countLeadingZeros32(1), 31)
await check("zero", countLeadingZeros32(0), 32)
await check("all ones", countLeadingZeros32(4294967295), 0)
await check("high bit", countLeadingZeros32(2147483648), 0)
await check("256", countLeadingZeros32(256), 23)
await check("16", countLeadingZeros32(16), 27)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
