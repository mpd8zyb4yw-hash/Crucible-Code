// Generated by generate.ts — hidden adversarial suite for kruskal-mst-weight.
// Run via `npx tsx __audit__/kruskalMstWeight.hidden.ts` inside the scratch project.
import { kruskalMstWeight } from '../src/kruskalMstWeight'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("classic", kruskalMstWeight(4,[[0,1,1],[0,2,4],[1,2,2],[1,3,5],[2,3,3]]), 6)
await check("no edges single", kruskalMstWeight(1,[]), 0)
await check("two nodes", kruskalMstWeight(2,[[0,1,7]]), 7)
await check("parallel edges picks min", kruskalMstWeight(2,[[0,1,5],[0,1,3]]), 3)
await check("disconnected forest", kruskalMstWeight(4,[[0,1,2],[2,3,3]]), 5)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
