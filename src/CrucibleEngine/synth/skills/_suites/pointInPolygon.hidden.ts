// Generated by generate.ts — hidden adversarial suite for point-in-polygon-raycast.
// Run via `npx tsx __audit__/pointInPolygon.hidden.ts` inside the scratch project.
import { pointInPolygon } from '../src/pointInPolygon'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("center of square", pointInPolygon(2,2,[[0,0],[4,0],[4,4],[0,4]]), true)
await check("outside right", pointInPolygon(5,5,[[0,0],[4,0],[4,4],[0,4]]), false)
await check("outside left", pointInPolygon(-1,2,[[0,0],[4,0],[4,4],[0,4]]), false)
await check("inside triangle", pointInPolygon(1,1,[[0,0],[4,0],[0,4]]), true)
await check("outside triangle", pointInPolygon(3,3,[[0,0],[4,0],[0,4]]), false)
await check("empty polygon", pointInPolygon(0,0,[]), false)
await check("far away", pointInPolygon(100,100,[[0,0],[4,0],[4,4],[0,4]]), false)
await check("left edge counted inside by half-open rule", pointInPolygon(0,2,[[0,0],[4,0],[4,4],[0,4]]), true)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
