// Generated by generate.ts — hidden adversarial suite for reverse-string.
// Run via `npx tsx __audit__/reverseString.hidden.ts` inside the scratch project.
import { reverseString } from '../src/reverseString'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("basic", reverseString("hello"), "olleh")
await check("palindrome", reverseString("racecar"), "racecar")
await check("empty", reverseString(""), "")
await check("single char", reverseString("a"), "a")
await check("with spaces", reverseString("hello world"), "dlrow olleh")
await check("numbers", reverseString("12345"), "54321")
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
