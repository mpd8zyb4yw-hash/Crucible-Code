// Generated by generate.ts — hidden adversarial suite for gray-code.
// Run via `npx tsx __audit__/grayCode.hidden.ts` inside the scratch project.
import { grayEncode, grayDecode } from '../src/grayCode'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("encode 0", grayEncode(0), 0)
await check("encode 2", grayEncode(2), 3)
await check("encode 3", grayEncode(3), 2)
await check("encode 4", grayEncode(4), 6)
await check("decode 6", grayDecode(6), 4)
await check("decode 2", grayDecode(2), 3)
await check("roundtrip", grayDecode(grayEncode(42)), 42)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
