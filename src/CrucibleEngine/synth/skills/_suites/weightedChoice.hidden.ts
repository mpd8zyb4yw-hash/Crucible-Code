// Generated by generate.ts — hidden adversarial suite for weighted-choice.
// Run via `npx tsx __audit__/weightedChoice.hidden.ts` inside the scratch project.
import { weightedChoice } from '../src/weightedChoice'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("first when r=0", weightedChoice(["a","b","c"],[1,1,1],0), "a")
await check("last when r near 1", weightedChoice(["a","b","c"],[1,1,1],0.9999), "c")
await check("heavy first", weightedChoice(["a","b"],[100,1],0), "a")
await check("single item", weightedChoice(["only"],[1],0.5), "only")
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
