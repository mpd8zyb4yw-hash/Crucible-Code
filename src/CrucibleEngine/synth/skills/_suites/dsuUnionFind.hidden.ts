// Generated by generate.ts — hidden adversarial suite for dsu-union-find.
// Run via `npx tsx __audit__/dsuUnionFind.hidden.ts` inside the scratch project.
import { DSU } from '../src/dsuUnionFind'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("union and connected", (() => { const d = new DSU(4); d.union(0,1); d.union(2,3); return d.connected(0,1) })(), true)
await check("not connected", (() => { const d = new DSU(4); d.union(0,1); return d.connected(0,2) })(), false)
await check("count init", new DSU(5).count(), 5)
await check("count after union", (() => { const d = new DSU(4); d.union(0,1); d.union(2,3); return d.count() })(), 2)
await check("self connected", (() => { const d = new DSU(3); return d.connected(1,1) })(), true)
await check("path compression works", (() => { const d = new DSU(5); d.union(0,1); d.union(1,2); d.union(2,3); return d.connected(0,3) })(), true)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
