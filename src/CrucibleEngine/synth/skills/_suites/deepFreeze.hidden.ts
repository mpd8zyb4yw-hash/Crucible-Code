// Generated by generate.ts — hidden adversarial suite for deep-freeze.
// Run via `npx tsx __audit__/deepFreeze.hidden.ts` inside the scratch project.
import { deepFreeze } from '../src/deepFreeze'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("top level frozen", (() => { const o = deepFreeze({a:1}); return Object.isFrozen(o) })(), true)
await check("nested frozen", (() => { const o = deepFreeze({a:{b:1}}); return Object.isFrozen(o.a) })(), true)
await check("returns original", (() => { const o = {a:1}; return deepFreeze(o) === o })(), true)
await check("primitive passthrough", deepFreeze(42), 42)
await check("null passthrough", deepFreeze(null), null)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
