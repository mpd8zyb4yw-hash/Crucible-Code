// Generated by generate.ts — hidden adversarial suite for coin-change-ways.
// Run via `npx tsx __audit__/coinChangeWays.hidden.ts` inside the scratch project.
import { coinChangeWays } from '../src/coinChangeWays'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("classic 5", coinChangeWays([1,2,5],5), 4)
await check("impossible", coinChangeWays([2],3), 0)
await check("zero amount", coinChangeWays([1,2,5],0), 1)
await check("only ones", coinChangeWays([1],4), 1)
await check("amount 3 with 1,2", coinChangeWays([1,2],3), 2)
await check("no coins positive", coinChangeWays([],5), 0)
await check("no coins zero", coinChangeWays([],0), 1)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
