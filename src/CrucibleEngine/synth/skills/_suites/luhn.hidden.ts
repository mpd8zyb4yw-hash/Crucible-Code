// Generated by generate.ts — hidden adversarial suite for luhn.
// Run via `npx tsx __audit__/luhn.hidden.ts` inside the scratch project.
import { luhn } from '../src/luhn'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("valid Visa", luhn("4532015112830366"), true)
await check("valid Mastercard", luhn("5425233430109903"), true)
await check("invalid", luhn("1234567890123456"), false)
await check("single zero", luhn("0"), true)
await check("with spaces", luhn("4532 0151 1283 0366"), true)
await check("with dashes", luhn("4532-0151-1283-0366"), true)
await check("empty", luhn(""), false)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
