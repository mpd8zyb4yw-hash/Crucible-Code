// Generated by generate.ts — hidden adversarial suite for mask-string.
// Run via `npx tsx __audit__/maskString.hidden.ts` inside the scratch project.
import { maskString } from '../src/maskString'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("basic", maskString("1234567890",4), "******7890")
await check("all visible", maskString("hello",10), "hello")
await check("none visible", maskString("hello",0), "*****")
await check("one visible", maskString("secret",1), "*****t")
await check("empty", maskString("",3), "")
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
