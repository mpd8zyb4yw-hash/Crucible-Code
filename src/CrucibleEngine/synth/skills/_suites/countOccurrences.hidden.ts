// Generated by generate.ts — hidden adversarial suite for count-occurrences.
// Run via `npx tsx __audit__/countOccurrences.hidden.ts` inside the scratch project.
import { countOccurrences } from '../src/countOccurrences'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("basic", countOccurrences("hello world hello", "hello"), 2)
await check("no match", countOccurrences("hello", "xyz"), 0)
await check("non-overlapping", countOccurrences("aaaa", "aa"), 2)
await check("single char", countOccurrences("banana", "a"), 3)
await check("empty sub", countOccurrences("hello", ""), 0)
await check("empty string", countOccurrences("", "a"), 0)
await check("sub longer than str", countOccurrences("hi", "hello"), 0)
await check("sub equals str", countOccurrences("abc", "abc"), 1)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
