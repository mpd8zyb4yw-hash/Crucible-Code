// Generated by generate.ts — hidden adversarial suite for title-case-smart.
// Run via `npx tsx __audit__/titleCaseSmart.hidden.ts` inside the scratch project.
import { titleCaseSmart } from '../src/titleCaseSmart'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("small word", titleCaseSmart("the quick brown fox"), "The Quick Brown Fox")
await check("keeps small mid", titleCaseSmart("war and peace"), "War and Peace")
await check("and at start", titleCaseSmart("and then"), "And Then")
await check("single word", titleCaseSmart("hello"), "Hello")
await check("empty", titleCaseSmart(""), "")
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
