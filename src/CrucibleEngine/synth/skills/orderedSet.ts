// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — OrderedSet stores unique values in insertion order with add, has, delete, values, and size.\nexport class OrderedSet<T> {\n  private s = new Set<T>()\n  add(item: T): void { this.s.add(item) }\n  has(item: T): boolean { return this.s.has(item) }\n  delete(item: T): boolean { return this.s.delete(item) }\n  values(): T[] { return [...this.s] }\n  size(): number { return this.s.size }\n}\n"

registerSkill({
  id: "ordered-set",
  summary: "OrderedSet stores unique values in insertion order with add, has, delete, values, and size.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bOrderedSet\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("ordered set|insertion order.*unique", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/orderedSet.ts", content: IMPL }]
  },
})
