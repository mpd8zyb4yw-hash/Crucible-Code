// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — addBusinessDays adds n business days (Mon-Fri) to a Date in UTC, returning a new Date.\nexport function addBusinessDays(date: Date, n: number): Date {\n  const d = new Date(Date.UTC(date.getUTCFullYear(), date.getUTCMonth(), date.getUTCDate()))\n  let remaining = n\n  while (remaining > 0) {\n    d.setUTCDate(d.getUTCDate() + 1)\n    const dow = d.getUTCDay()\n    if (dow !== 0 && dow !== 6) remaining--\n  }\n  return d\n}\n"

registerSkill({
  id: "add-business-days",
  summary: "addBusinessDays adds n business days (Mon-Fri) to a Date in UTC, returning a new Date.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\baddBusinessDays\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("add.*business.*days?|business.*days?.*add", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/addBusinessDays.ts", content: IMPL }]
  },
})
