// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — Compute the Euclidean distance between two 2D points using distancePoints.\nexport function distancePoints(ax: number, ay: number, bx: number, by: number): number {\n  return Math.hypot(bx - ax, by - ay);\n}\n\n"

registerSkill({
  id: "euclidean-distance-points",
  summary: "Compute the Euclidean distance between two 2D points using distancePoints.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bdistancePoints\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("[Ee]uclidean", 'i'))) sc += 0.2
  if (s.has(new RegExp("\\bdistance\\b", 'i'))) sc += 0.15
  if (s.has(new RegExp("\\bpoints?\\b", 'i'))) sc += 0.1
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/distancePoints.ts", content: IMPL }]
  },
})
