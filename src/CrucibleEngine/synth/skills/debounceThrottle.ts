// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — Debounce and throttle: delay or rate-limit repeated calls.\nexport function debounce<T extends (...args: unknown[]) => unknown>(\n  fn: T, waitMs: number, now: () => number = Date.now\n): T & { cancel(): void; flush(): void } {\n  let timer: ReturnType<typeof setTimeout> | null = null\n  let last: unknown\n  const debounced = (...args: unknown[]) => {\n    if (timer) clearTimeout(timer)\n    timer = setTimeout(() => { timer = null; last = fn(...args) }, waitMs)\n  }\n  debounced.cancel = () => { if (timer) { clearTimeout(timer); timer = null } }\n  debounced.flush = () => { if (timer) { clearTimeout(timer); timer = null } }\n  return debounced as T & { cancel(): void; flush(): void }\n}\n\nexport function throttle<T extends (...args: unknown[]) => unknown>(\n  fn: T, waitMs: number, now: () => number = Date.now\n): T & { cancel(): void } {\n  let last = -Infinity\n  const throttled = (...args: unknown[]) => {\n    const t = now()\n    if (t - last >= waitMs) { last = t; return fn(...args) }\n  }\n  throttled.cancel = () => { last = -Infinity }\n  return throttled as T & { cancel(): void }\n}\n"

registerSkill({
  id: "debounce-throttle",
  summary: "Debounce and throttle: delay or rate-limit repeated calls.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bdebounce\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("\\bthrottle\\b", 'i'))) sc += 0.5
  if (s.has(new RegExp("\\bdebounce.*throttle|throttle.*debounce", 'i'))) sc += 0.65
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/debounceThrottle.ts", content: IMPL }]
  },
})
