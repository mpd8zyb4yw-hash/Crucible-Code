// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — connectedComponentsCount returns the number of connected components in an undirected graph.\nexport function connectedComponentsCount(n, edges) {\n  const parent = Array.from({ length: n }, (_, i) => i)\n  const find = (x) => { while (parent[x] !== x) { parent[x] = parent[parent[x]]; x = parent[x] } return x }\n  let count = n\n  for (const [u, v] of edges) { const ru = find(u), rv = find(v); if (ru !== rv) { parent[ru] = rv; count-- } }\n  return count\n}\n"

registerSkill({
  id: "connected-components-count",
  summary: "connectedComponentsCount returns the number of connected components in an undirected graph.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bconnectedComponentsCount\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("connected component", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/connectedComponentsCount.ts", content: IMPL }]
  },
})
