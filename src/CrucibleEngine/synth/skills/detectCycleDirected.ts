// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — detectCycleDirected reports whether a directed graph has a cycle.\nexport function detectCycleDirected(n, edges) {\n  const adj = Array.from({ length: n }, () => [])\n  for (const [u, v] of edges) adj[u].push(v)\n  const WHITE = 0, GREY = 1, BLACK = 2\n  const color = new Array(n).fill(WHITE)\n  const dfs = (u) => {\n    color[u] = GREY\n    for (const v of adj[u]) {\n      if (color[v] === GREY) return true\n      if (color[v] === WHITE && dfs(v)) return true\n    }\n    color[u] = BLACK; return false\n  }\n  for (let i = 0; i < n; i++) if (color[i] === WHITE && dfs(i)) return true\n  return false\n}\n"

registerSkill({
  id: "detect-cycle-directed",
  summary: "detectCycleDirected reports whether a directed graph has a cycle.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bdetectCycleDirected\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("cycle.*directed|directed.*cycle", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/detectCycleDirected.ts", content: IMPL }]
  },
})
