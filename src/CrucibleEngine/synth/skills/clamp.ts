// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — Clamp a number between min and max; lerp between two values; round to N decimals.\nexport function clamp(value: number, min: number, max: number): number {\n  return Math.max(min, Math.min(max, value))\n}\n\nexport function lerp(a: number, b: number, t: number): number {\n  return a + (b - a) * t\n}\n\nexport function roundTo(value: number, decimals: number): number {\n  const factor = 10 ** decimals\n  return Math.round(value * factor) / factor\n}\n"

registerSkill({
  id: "clamp",
  summary: "Clamp a number between min and max; lerp between two values; round to N decimals.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bclamp\\b", 'i'))) sc += 0.65
  if (s.has(new RegExp("\\blerp\\b|linear.*interpolat", 'i'))) sc += 0.5
  if (s.has(new RegExp("\\broundTo\\b|round.*decimal", 'i'))) sc += 0.4
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/clamp.ts", content: IMPL }]
  },
})
