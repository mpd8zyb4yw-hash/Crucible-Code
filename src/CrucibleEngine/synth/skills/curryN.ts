// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — curryN curries a function to a fixed arity, collecting arguments until the arity is reached.\nexport function curryN(fn: (...args: any[]) => any, arity: number): (...args: any[]) => any {\n  return function curried(...args: any[]): any {\n    if (args.length >= arity) return fn(...args.slice(0, arity))\n    return (...more: any[]) => curried(...args, ...more)\n  }\n}\n"

registerSkill({
  id: "curry-n",
  summary: "curryN curries a function to a fixed arity, collecting arguments until the arity is reached.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bcurryN\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("curry.*arity|curry.*function", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/curryN.ts", content: IMPL }]
  },
})
