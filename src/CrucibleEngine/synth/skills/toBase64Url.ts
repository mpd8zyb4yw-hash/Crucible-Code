// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — toBase64Url encodes a string to URL-safe base64 (no padding) and fromBase64Url decodes it; they roundtrip.\nexport function toBase64Url(s: string): string {\n  return Buffer.from(s,'utf8').toString('base64').replace(/\\+/g,'-').replace(/\\//g,'_').replace(/=/g,'')\n}\nexport function fromBase64Url(s: string): string {\n  const padded = s.replace(/-/g,'+').replace(/_/g,'/') + '==='.slice((s.length + 3) % 4)\n  return Buffer.from(padded,'base64').toString('utf8')\n}\n"

registerSkill({
  id: "to-base64-url",
  summary: "toBase64Url encodes a string to URL-safe base64 (no padding) and fromBase64Url decodes it; they roundtrip.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\btoBase64Url\\b|\\bfromBase64Url\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("base64.*url|url.?safe.*base64", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/toBase64Url.ts", content: IMPL }]
  },
})
