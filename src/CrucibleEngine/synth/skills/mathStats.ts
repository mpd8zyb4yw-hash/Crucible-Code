// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — sum, average, median, mode of a number array.\nexport function sum(arr: number[]): number {\n  return arr.reduce((a, b) => a + b, 0)\n}\n\nexport function average(arr: number[]): number {\n  if (!arr.length) return NaN\n  return sum(arr) / arr.length\n}\n\nexport function median(arr: number[]): number {\n  if (!arr.length) return NaN\n  const s = [...arr].sort((a, b) => a - b)\n  const mid = Math.floor(s.length / 2)\n  return s.length % 2 === 0 ? (s[mid - 1] + s[mid]) / 2 : s[mid]\n}\n\nexport function mode(arr: number[]): number[] {\n  if (!arr.length) return []\n  const freq = new Map<number, number>()\n  for (const n of arr) freq.set(n, (freq.get(n) ?? 0) + 1)\n  const max = Math.max(...freq.values())\n  return [...freq.entries()].filter(([, v]) => v === max).map(([k]) => k).sort((a, b) => a - b)\n}\n"

registerSkill({
  id: "math-stats",
  summary: "sum, average, median, mode of a number array.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bsum\\b.*array|array.*\\bsum\\b", 'i'))) sc += 0.4
  if (s.has(new RegExp("\\baverage\\b|\\bmean\\b", 'i'))) sc += 0.5
  if (s.has(new RegExp("\\bmedian\\b", 'i'))) sc += 0.55
  if (s.has(new RegExp("\\bmode\\b.*array|most.*frequent.*number", 'i'))) sc += 0.5
  if (s.has(new RegExp("\\bsum.*average|average.*median|median.*mode", 'i'))) sc += 0.6
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/mathStats.ts", content: IMPL }]
  },
})
