// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — countLeadingZeros32 returns the number of leading zero bits in the 32-bit representation of n (32 for zero).\nexport function countLeadingZeros32(n: number): number {\n  return n === 0 ? 32 : Math.clz32(n)\n}\n"

registerSkill({
  id: "count-leading-zeros-32",
  summary: "countLeadingZeros32 returns the number of leading zero bits in the 32-bit representation of n (32 for zero).",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bcountLeadingZeros32\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("leading zero|count.*zeros", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/countLeadingZeros32.ts", content: IMPL }]
  },
})
