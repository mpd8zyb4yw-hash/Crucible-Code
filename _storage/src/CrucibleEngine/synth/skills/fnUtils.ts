// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — Function utilities: once, compose, pipe, debounce, throttle.\nexport function once<T extends (...args: unknown[]) => unknown>(fn: T): T {\n  let called = false, result: unknown\n  return ((...args: unknown[]) => {\n    if (!called) { called = true; result = fn(...args) }\n    return result\n  }) as T\n}\n\nexport function compose<T>(...fns: Array<(arg: T) => T>): (arg: T) => T {\n  return (arg: T) => fns.reduceRight((v, f) => f(v), arg)\n}\n\nexport function pipe<T>(...fns: Array<(arg: T) => T>): (arg: T) => T {\n  return (arg: T) => fns.reduce((v, f) => f(v), arg)\n}\n"

registerSkill({
  id: "fn-utils",
  summary: "Function utilities: once, compose, pipe, debounce, throttle.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bonce\\b.*function|function.*\\bonce\\b", 'i'))) sc += 0.55
  if (s.has(new RegExp("\\bcompose\\b.*function|function.*\\bcompose\\b", 'i'))) sc += 0.5
  if (s.has(new RegExp("\\bpipe\\b.*function|function.*\\bpipe\\b", 'i'))) sc += 0.45
  if (s.has(new RegExp("\\bcompose.*pipe|pipe.*compose", 'i'))) sc += 0.5
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/fnUtils.ts", content: IMPL }]
  },
})
