// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — movingMedian computes the median over each rolling window of length k across a list of numbers; returns an empty list when k exceeds the input length.\nexport function movingMedian(arr: number[], window: number): number[] {\n  const n = arr.length;\n  if (window <= 0 || window > n) return [];\n  const out: number[] = [];\n  for (let i = 0; i + window <= n; i++) {\n    const w = arr.slice(i, i + window).sort((a, b) => a - b);\n    const mid = Math.floor(window / 2);\n    out.push(window % 2 === 1 ? w[mid] : (w[mid - 1] + w[mid]) / 2);\n  }\n  return out;\n}\n\n"

registerSkill({
  id: "moving-median-window",
  summary: "movingMedian computes the median over each rolling window of length k across a list of numbers; returns an empty list when k exceeds the input length.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bmovingMedian\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("rolling window", 'i'))) sc += 0.2
  if (s.has(new RegExp("\\bmedian\\b", 'i'))) sc += 0.2
  if (s.has(new RegExp("list of numbers", 'i'))) sc += 0.1
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/movingMedianWindow.ts", content: IMPL }]
  },
})
