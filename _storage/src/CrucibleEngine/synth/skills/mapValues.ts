// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — Map over object values; map over object keys; filter object entries.\nexport function mapValues<T, U>(\n  obj: Record<string, T>,\n  fn: (value: T, key: string) => U,\n): Record<string, U> {\n  const out: Record<string, U> = {}\n  for (const [k, v] of Object.entries(obj)) out[k] = fn(v, k)\n  return out\n}\n\nexport function mapKeys<T>(\n  obj: Record<string, T>,\n  fn: (key: string, value: T) => string,\n): Record<string, T> {\n  const out: Record<string, T> = {}\n  for (const [k, v] of Object.entries(obj)) out[fn(k, v)] = v\n  return out\n}\n\nexport function filterValues<T>(\n  obj: Record<string, T>,\n  pred: (value: T, key: string) => boolean,\n): Record<string, T> {\n  const out: Record<string, T> = {}\n  for (const [k, v] of Object.entries(obj)) if (pred(v, k)) out[k] = v\n  return out\n}\n"

registerSkill({
  id: "map-values",
  summary: "Map over object values; map over object keys; filter object entries.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bmapValues\\b|\\bmap.*values\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("\\bmapKeys\\b|\\bmap.*keys\\b", 'i'))) sc += 0.5
  if (s.has(new RegExp("\\bfilterValues\\b|\\bfilter.*object", 'i'))) sc += 0.4
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/mapValues.ts", content: IMPL }]
  },
})
