// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — matrixDeterminant computes the determinant of a square numeric matrix via cofactor expansion.\nexport function matrixDeterminant(m: number[][]): number {\n  const n = m.length\n  if (n === 0) return 1\n  if (n === 1) return m[0][0]\n  if (n === 2) return m[0][0] * m[1][1] - m[0][1] * m[1][0]\n  let det = 0\n  for (let c = 0; c < n; c++) {\n    const minor = m.slice(1).map(row => row.filter((_, j) => j !== c))\n    det += (c % 2 ? -1 : 1) * m[0][c] * matrixDeterminant(minor)\n  }\n  return det\n}\n"

registerSkill({
  id: "matrix-determinant",
  summary: "matrixDeterminant computes the determinant of a square numeric matrix via cofactor expansion.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bmatrixDeterminant\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("determinant", 'i'))) sc += 0.35
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/matrixDeterminant.ts", content: IMPL }]
  },
})
