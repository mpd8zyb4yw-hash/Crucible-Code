// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — Validate a URL (http or https).\nexport function isURL(str: string): boolean {\n  try { const u = new URL(str); return u.protocol === 'http:' || u.protocol === 'https:' } catch { return false }\n}\n"

registerSkill({
  id: "is-url",
  summary: "Validate a URL (http or https).",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bvalidate.*url|url.*validat", 'i'))) sc += 0.6
  if (s.has(new RegExp("\\bisURL\\b|\\bis[- ]?url\\b", 'i'))) sc += 0.4
  if (s.has(new RegExp("\\bhttp.*url.*check|url.*format.*check", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/isURL.ts", content: IMPL }]
  },
})
