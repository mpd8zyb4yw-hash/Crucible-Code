// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — matrixTranspose returns the transpose of a numeric matrix, handling non-square and empty inputs.\nexport function matrixTranspose(m: number[][]): number[][] {\n  if (!m.length) return []\n  return m[0].map((_, c) => m.map(row => row[c]))\n}\n"

registerSkill({
  id: "matrix-transpose",
  summary: "matrixTranspose returns the transpose of a numeric matrix, handling non-square and empty inputs.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bmatrixTranspose\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("transpose", 'i'))) sc += 0.35
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/matrixTranspose.ts", content: IMPL }]
  },
})
