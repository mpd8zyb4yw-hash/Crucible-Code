// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — DSU (disjoint-set union) with constructor, find (path compression), union, connected, and count.\nexport class DSU {\n  private parent\n  private rank\n  private cnt\n  constructor(n) {\n    this.parent = Array.from({ length: n }, (_, i) => i)\n    this.rank = new Array(n).fill(0)\n    this.cnt = n\n  }\n  find(x) {\n    while (this.parent[x] !== x) { this.parent[x] = this.parent[this.parent[x]]; x = this.parent[x] }\n    return x\n  }\n  union(a, b) {\n    const ra = this.find(a), rb = this.find(b)\n    if (ra === rb) return\n    if (this.rank[ra] < this.rank[rb]) this.parent[ra] = rb\n    else if (this.rank[ra] > this.rank[rb]) this.parent[rb] = ra\n    else { this.parent[rb] = ra; this.rank[ra]++ }\n    this.cnt--\n  }\n  connected(a, b) { return this.find(a) === this.find(b) }\n  count() { return this.cnt }\n}\n"

registerSkill({
  id: "dsu-union-find",
  summary: "DSU (disjoint-set union) with constructor, find (path compression), union, connected, and count.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bDSU\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("disjoint.?set|union.?find", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/dsuUnionFind.ts", content: IMPL }]
  },
})
