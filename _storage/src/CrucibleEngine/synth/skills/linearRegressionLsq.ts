// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — linearRegression computes the least-squares slope and intercept fit for two equal-length numeric arrays.\nexport function linearRegression(x: number[], y: number[]): { slope: number; intercept: number } {\n  const n = x.length;\n  let sx = 0, sy = 0, sxy = 0, sxx = 0;\n  for (let i = 0; i < n; i++) {\n    sx += x[i];\n    sy += y[i];\n    sxy += x[i] * y[i];\n    sxx += x[i] * x[i];\n  }\n  const slope = (n * sxy - sx * sy) / (n * sxx - sx * sx);\n  const intercept = (sy - slope * sx) / n;\n  return { slope, intercept };\n}\n\n"

registerSkill({
  id: "linear-regression-lsq",
  summary: "linearRegression computes the least-squares slope and intercept fit for two equal-length numeric arrays.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\blinearRegression\\b", 'i'))) sc += 0.5
  if (s.has(new RegExp("least-squares", 'i'))) sc += 0.2
  if (s.has(new RegExp("slope and intercept", 'i'))) sc += 0.15
  if (s.has(new RegExp("regression", 'i'))) sc += 0.1
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/linearRegressionLsq.ts", content: IMPL }]
  },
})
