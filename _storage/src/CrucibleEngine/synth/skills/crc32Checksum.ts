// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — crc32 computes the CRC-32 checksum of a string as an unsigned 32-bit integer.\nconst TABLE = new Uint32Array(256)\nfor (let i = 0; i < 256; i++) { let c = i; for (let k = 0; k < 8; k++) c = (c & 1) ? 0xEDB88320 ^ (c >>> 1) : c >>> 1; TABLE[i] = c }\nexport function crc32(str: string): number {\n  let crc = 0xFFFFFFFF\n  for (const ch of Buffer.from(str, 'utf8')) crc = TABLE[(crc ^ ch) & 0xFF] ^ (crc >>> 8)\n  return (crc ^ 0xFFFFFFFF) >>> 0\n}\n"

registerSkill({
  id: "crc32-checksum",
  summary: "crc32 computes the CRC-32 checksum of a string as an unsigned 32-bit integer.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bcrc32\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("crc.?32|cyclic redundancy", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/crc32Checksum.ts", content: IMPL }]
  },
})
