// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — assertNever, isError, tryCatch, Result type with ok/err helpers.\nexport type Result<T, E = Error> = { ok: true; value: T } | { ok: false; error: E }\n\nexport function ok<T>(value: T): Result<T, never> { return { ok: true, value } }\nexport function err<E>(error: E): Result<never, E> { return { ok: false, error } }\n\nexport function isError(val: unknown): val is Error {\n  return val instanceof Error\n}\n\nexport function tryCatch<T>(fn: () => T): Result<T, Error> {\n  try { return ok(fn()) } catch (e) { return err(e instanceof Error ? e : new Error(String(e))) }\n}\n\nexport async function tryCatchAsync<T>(fn: () => Promise<T>): Promise<Result<T, Error>> {\n  try { return ok(await fn()) } catch (e) { return err(e instanceof Error ? e : new Error(String(e))) }\n}\n"

registerSkill({
  id: "type-utils",
  summary: "assertNever, isError, tryCatch, Result type with ok/err helpers.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bResult.*type|\\bResult<", 'i'))) sc += 0.55
  if (s.has(new RegExp("\\btryCatch\\b|\\btry.*catch.*util", 'i'))) sc += 0.55
  if (s.has(new RegExp("\\bisError\\b|\\berror.*util", 'i'))) sc += 0.5
  if (s.has(new RegExp("\\bok\\b.*\\berr\\b.*result|result.*type.*ok.*err", 'i'))) sc += 0.55
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/typeUtils.ts", content: IMPL }]
  },
})
