// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — zFunctionArray computes the Z-array of a string where z[i] is the longest common prefix of s and s[i:]; z[0] is the full length.\nexport function zFunctionArray(s: string): number[] {\n  const n = s.length\n  const z = new Array(n).fill(0)\n  if (n === 0) return z\n  z[0] = n\n  let l = 0, r = 0\n  for (let i = 1; i < n; i++) {\n    if (i < r) z[i] = Math.min(r - i, z[i - l])\n    while (i + z[i] < n && s[z[i]] === s[i + z[i]]) z[i]++\n    if (i + z[i] > r) { l = i; r = i + z[i] }\n  }\n  return z\n}\n"

registerSkill({
  id: "z-function-array",
  summary: "zFunctionArray computes the Z-array of a string where z[i] is the longest common prefix of s and s[i:]; z[0] is the full length.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bzFunctionArray\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("z.?function|z.?array|z.?algorithm", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/zFunctionArray.ts", content: IMPL }]
  },
})
