// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — Merge sort, quick sort, and insertion sort for number arrays.\nexport function mergeSort(arr: number[]): number[] {\n  if (arr.length <= 1) return arr\n  const mid = arr.length >> 1\n  const left = mergeSort(arr.slice(0, mid))\n  const right = mergeSort(arr.slice(mid))\n  const out: number[] = []\n  let i = 0, j = 0\n  while (i < left.length && j < right.length) {\n    left[i] <= right[j] ? out.push(left[i++]) : out.push(right[j++])\n  }\n  return out.concat(left.slice(i), right.slice(j))\n}\n\nexport function quickSort(arr: number[]): number[] {\n  if (arr.length <= 1) return arr\n  const pivot = arr[arr.length >> 1]\n  const left = arr.filter(x => x < pivot)\n  const mid = arr.filter(x => x === pivot)\n  const right = arr.filter(x => x > pivot)\n  return [...quickSort(left), ...mid, ...quickSort(right)]\n}\n\nexport function insertionSort(arr: number[]): number[] {\n  const out = [...arr]\n  for (let i = 1; i < out.length; i++) {\n    const key = out[i]; let j = i - 1\n    while (j >= 0 && out[j] > key) { out[j + 1] = out[j]; j-- }\n    out[j + 1] = key\n  }\n  return out\n}\n"

registerSkill({
  id: "sorting",
  summary: "Merge sort, quick sort, and insertion sort for number arrays.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bmerge.*sort\\b", 'i'))) sc += 0.65
  if (s.has(new RegExp("\\bquick.*sort\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("\\binsertion.*sort\\b", 'i'))) sc += 0.55
  if (s.has(new RegExp("\\bmergeSort\\b|\\bquickSort\\b", 'i'))) sc += 0.4
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/sorting.ts", content: IMPL }]
  },
})
