// Generated by generate.ts — hidden adversarial suite for path-utils.
// Run via `npx tsx __audit__/pathUtils.hidden.ts` inside the scratch project.
import { joinPath, getExtension, getFileName, getDirName, stripExtension, normalizePath } from '../src/pathUtils'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("joinPath basic", joinPath("src", "utils", "foo.ts"), "src/utils/foo.ts")
await check("joinPath trailing slash", joinPath("src/", "/utils"), "src/utils")
await check("getExtension .ts", getExtension("foo.ts"), ".ts")
await check("getExtension .tar.gz", getExtension("file.tar.gz"), ".gz")
await check("getExtension no ext", getExtension("Makefile"), "")
await check("getFileName basic", getFileName("/src/utils/foo.ts"), "foo.ts")
await check("getDirName basic", getDirName("/src/utils/foo.ts"), "/src/utils")
await check("getDirName no dir", getDirName("foo.ts"), "/")
await check("stripExtension", stripExtension("foo.ts"), "foo")
await check("normalizePath dotdot", normalizePath("src/../lib/foo.ts"), "lib/foo.ts")
await check("normalizePath dot", normalizePath("./src/./foo"), "src/foo")
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
