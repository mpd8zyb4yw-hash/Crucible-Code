// Generated by generate.ts — hidden adversarial suite for date-utils.
// Run via `npx tsx __audit__/dateUtils.hidden.ts` inside the scratch project.
import { addDays, diffDays, isLeapYear, daysInMonth, startOfDay, endOfDay } from '../src/dateUtils'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("addDays basic", addDays(new Date("2024-01-01T00:00:00Z"), 5).toISOString().slice(0,10), "2024-01-06")
await check("addDays month boundary", addDays(new Date("2024-01-30T00:00:00Z"), 3).toISOString().slice(0,10), "2024-02-02")
await check("addDays negative", addDays(new Date("2024-01-10T00:00:00Z"), -3).toISOString().slice(0,10), "2024-01-07")
await check("diffDays positive", diffDays(new Date("2024-01-01T00:00:00Z"), new Date("2024-01-06T00:00:00Z")), 5)
await check("diffDays negative", diffDays(new Date("2024-01-06T00:00:00Z"), new Date("2024-01-01T00:00:00Z")), -5)
await check("diffDays same", diffDays(new Date("2024-01-01T00:00:00Z"), new Date("2024-01-01T00:00:00Z")), 0)
await check("isLeapYear 2024", isLeapYear(2024), true)
await check("isLeapYear 2023", isLeapYear(2023), false)
await check("isLeapYear 1900", isLeapYear(1900), false)
await check("isLeapYear 2000", isLeapYear(2000), true)
await check("daysInMonth Jan", daysInMonth(2024, 1), 31)
await check("daysInMonth Feb leap", daysInMonth(2024, 2), 29)
await check("daysInMonth Feb non-leap", daysInMonth(2023, 2), 28)
await check("startOfDay midnight", startOfDay(new Date("2024-01-15T14:30:00Z")).toISOString(), "2024-01-15T00:00:00.000Z")
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
