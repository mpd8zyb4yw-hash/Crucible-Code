// Generated by generate.ts — hidden adversarial suite for detect-cycle-directed.
// Run via `npx tsx __audit__/detectCycleDirected.hidden.ts` inside the scratch project.
import { detectCycleDirected } from '../src/detectCycleDirected'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("no cycle", detectCycleDirected(3,[[0,1],[1,2]]), false)
await check("has cycle", detectCycleDirected(3,[[0,1],[1,2],[2,0]]), true)
await check("self loop", detectCycleDirected(2,[[0,0]]), true)
await check("no edges", detectCycleDirected(3,[]), false)
await check("diamond no cycle", detectCycleDirected(4,[[0,1],[0,2],[1,3],[2,3]]), false)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
