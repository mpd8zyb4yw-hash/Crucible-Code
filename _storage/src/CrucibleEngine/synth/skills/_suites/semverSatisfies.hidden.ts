// Generated by generate.ts — hidden adversarial suite for semver-satisfies.
// Run via `npx tsx __audit__/semverSatisfies.hidden.ts` inside the scratch project.
import { semverSatisfies } from '../src/semverSatisfies'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("exact match satisfied", semverSatisfies("1.2.3","1.2.3"), true)
await check("exact mismatch fails", semverSatisfies("1.2.4","1.2.3"), false)
await check("caret allows higher minor", semverSatisfies("1.5.0","^1.2.3"), true)
await check("caret blocks major bump", semverSatisfies("2.0.0","^1.2.3"), false)
await check("caret blocks lower than base", semverSatisfies("1.2.2","^1.2.3"), false)
await check("caret 0.x locks minor and allows patch", semverSatisfies("0.2.5","^0.2.3"), true)
await check("caret 0.x blocks minor bump", semverSatisfies("0.3.0","^0.2.3"), false)
await check("tilde allows patch", semverSatisfies("1.2.9","~1.2.3"), true)
await check("tilde blocks minor bump", semverSatisfies("1.3.0","~1.2.3"), false)
await check("gte satisfied by higher", semverSatisfies("2.0.0",">=1.2.3"), true)
await check("gte fails for lower", semverSatisfies("1.0.0",">=1.2.3"), false)
await check("malformed version fails", semverSatisfies("1.2",">=1.0.0"), false)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
