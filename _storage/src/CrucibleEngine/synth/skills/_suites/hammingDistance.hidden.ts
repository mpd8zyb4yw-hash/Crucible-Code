// Generated by generate.ts — hidden adversarial suite for hamming-distance.
// Run via `npx tsx __audit__/hammingDistance.hidden.ts` inside the scratch project.
import { hammingDistance } from '../src/hammingDistance'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("1 vs 4", hammingDistance(1,4), 2)
await check("zero", hammingDistance(0,0), 0)
await check("7 vs 0", hammingDistance(7,0), 3)
await check("equal", hammingDistance(5,5), 0)
await check("one bit", hammingDistance(1,0), 1)
await check("byte", hammingDistance(255,0), 8)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
