// Generated by generate.ts — hidden adversarial suite for is-url.
// Run via `npx tsx __audit__/isURL.hidden.ts` inside the scratch project.
import { isURL } from '../src/isURL'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("valid http", isURL("http://example.com"), true)
await check("valid https", isURL("https://example.com/path?q=1"), true)
await check("no protocol", isURL("example.com"), false)
await check("ftp rejected", isURL("ftp://example.com"), false)
await check("empty", isURL(""), false)
await check("just scheme", isURL("https://"), false)
await check("localhost", isURL("http://localhost:3000"), true)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
