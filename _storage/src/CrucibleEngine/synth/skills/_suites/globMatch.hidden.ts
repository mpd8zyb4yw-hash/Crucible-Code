// Generated by generate.ts — hidden adversarial suite for glob-match.
// Run via `npx tsx __audit__/globMatch.hidden.ts` inside the scratch project.
import { globMatch } from '../src/globMatch'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("star wildcard", globMatch("*.ts","foo.ts"), true)
await check("no match", globMatch("*.ts","foo.js"), false)
await check("question", globMatch("?.ts","a.ts"), true)
await check("question too long", globMatch("?.ts","ab.ts"), false)
await check("exact", globMatch("hello","hello"), true)
await check("star all", globMatch("*","anything"), true)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
