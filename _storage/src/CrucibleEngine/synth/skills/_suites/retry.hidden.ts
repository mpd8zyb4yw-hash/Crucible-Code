// Generated by generate.ts — hidden adversarial suite for retry.
// Run via `npx tsx __audit__/retry.hidden.ts` inside the scratch project.
import { retry } from '../src/retry'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("succeeds first try", (async()=>await retry(()=>Promise.resolve(42)))(), 42)
await check("succeeds after retry", (async()=>{let n=0;return await retry(()=>++n<3?Promise.reject("e"):Promise.resolve(n),{attempts:3,delayMs:0})})(), 3)
await check("throws after max attempts", (async()=>{try{await retry(()=>Promise.reject("fail"),{attempts:2,delayMs:0})}catch(e){return e}})(), "fail")
await check("onRetry called", (async()=>{let calls=0;try{await retry(()=>Promise.reject("e"),{attempts:3,delayMs:0,onRetry:()=>calls++})}catch{}return calls})(), 2)
await check("default 3 attempts", (async()=>{let n=0;try{await retry(()=>{n++;return Promise.reject("e")},{delayMs:0})}catch{}return n})(), 3)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
