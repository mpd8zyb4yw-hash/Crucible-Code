// Generated by generate.ts — hidden adversarial suite for pascal-case.
// Run via `npx tsx __audit__/pascalCase.hidden.ts` inside the scratch project.
import { pascalCase } from '../src/pascalCase'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("kebab to pascal", pascalCase("hello-world"), "HelloWorld")
await check("snake to pascal", pascalCase("hello_world"), "HelloWorld")
await check("space to pascal", pascalCase("hello world"), "HelloWorld")
await check("camelCase input", pascalCase("helloWorld"), "HelloWorld")
await check("already pascal", pascalCase("HelloWorld"), "HelloWorld")
await check("single word", pascalCase("hello"), "Hello")
await check("empty", pascalCase(""), "")
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
