// Generated by generate.ts — hidden adversarial suite for is-rotation-of.
// Run via `npx tsx __audit__/isRotationOf.hidden.ts` inside the scratch project.
import { isRotationOf } from '../src/isRotationOf'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("classic", isRotationOf("waterbottle","erbottlewat"), true)
await check("simple rotation", isRotationOf("abc","cab"), true)
await check("not rotation", isRotationOf("abc","acb"), false)
await check("both empty", isRotationOf("",""), true)
await check("length mismatch", isRotationOf("abc","ab"), false)
await check("identical", isRotationOf("aa","aa"), true)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
