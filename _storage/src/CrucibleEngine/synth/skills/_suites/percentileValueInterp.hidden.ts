// Generated by generate.ts — hidden adversarial suite for percentile-value-interp.
// Run via `npx tsx __audit__/percentileValueInterp.hidden.ts` inside the scratch project.
import { percentileValue } from '../src/percentileValueInterp'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("empty array yields NaN", Number.isNaN(percentileValue([], 50)), true)
await check("single element returns it", percentileValue([10], 50), 10)
await check("median interpolates between ranks", percentileValue([1,2,3,4], 50), 2.5)
await check("zeroth percentile is min", percentileValue([1,2,3,4], 0), 1)
await check("hundredth percentile is max", percentileValue([1,2,3,4], 100), 4)
await check("sorts an unsorted copy", percentileValue([3,1,2], 50), 2)
await check("quartile lands on exact rank", percentileValue([1,2,3,4,5], 25), 2)
await check("upper quartile exact rank", percentileValue([1,2,3,4,5], 75), 4)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
