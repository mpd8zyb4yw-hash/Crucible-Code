// Generated by generate.ts — hidden adversarial suite for event-emitter-simple.
// Run via `npx tsx __audit__/eventEmitterSimple.hidden.ts` inside the scratch project.
import { EventEmitter } from '../src/eventEmitterSimple'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("on and emit", (() => { const e = new EventEmitter(); let x = 0; e.on("a", () => x++); e.emit("a"); return x })(), 1)
await check("off removes listener", (() => { const e = new EventEmitter(); let x = 0; const fn = () => x++; e.on("a", fn); e.off("a", fn); e.emit("a"); return x })(), 0)
await check("once fires once", (() => { const e = new EventEmitter(); let x = 0; e.once("a", () => x++); e.emit("a"); e.emit("a"); return x })(), 1)
await check("emit returns true when listeners", (() => { const e = new EventEmitter(); e.on("a", ()=>{}); return e.emit("a") })(), true)
await check("emit returns false no listeners", new EventEmitter().emit("x"), false)
await check("listenerCount", (() => { const e = new EventEmitter(); e.on("a",()=>{}); e.on("a",()=>{}); return e.listenerCount("a") })(), 2)
await check("passes args", (() => { const e = new EventEmitter(); let got: unknown; e.on("a", (v) => got = v); e.emit("a", 42); return got })(), 42)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
