// Generated by generate.ts — hidden adversarial suite for hex-encode.
// Run via `npx tsx __audit__/hexEncode.hidden.ts` inside the scratch project.
import { hexEncode, hexDecode } from '../src/hexEncode'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("encode hello", hexEncode("hello"), "68656c6c6f")
await check("encode empty", hexEncode(""), "")
await check("decode hello", hexDecode("68656c6c6f"), "hello")
await check("roundtrip", hexDecode(hexEncode("Hello World!")), "Hello World!")
await check("roundtrip unicode", hexDecode(hexEncode("café")), "café")
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
