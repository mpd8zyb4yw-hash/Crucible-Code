// Generated by generate.ts — hidden adversarial suite for rot13.
// Run via `npx tsx __audit__/rot13.hidden.ts` inside the scratch project.
import { rot13 } from '../src/rot13'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("hello", rot13("hello"), "uryyb")
await check("HELLO", rot13("HELLO"), "URYYB")
await check("involution", rot13(rot13("Hello, World!")), "Hello, World!")
await check("preserves non-alpha", rot13("Hello, 123!"), "Uryyb, 123!")
await check("empty", rot13(""), "")
await check("mixed case", rot13("aBcDeFg"), "nOpQrSt")
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
