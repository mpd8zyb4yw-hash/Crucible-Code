// Generated by generate.ts — hidden adversarial suite for matrix-determinant.
// Run via `npx tsx __audit__/matrixDeterminant.hidden.ts` inside the scratch project.
import { matrixDeterminant } from '../src/matrixDeterminant'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("2x2", matrixDeterminant([[1,2],[3,4]]), -2)
await check("1x1", matrixDeterminant([[5]]), 5)
await check("identity 3x3", matrixDeterminant([[1,0,0],[0,1,0],[0,0,1]]), 1)
await check("3x3", matrixDeterminant([[6,1,1],[4,-2,5],[2,8,7]]), -306)
await check("singular", matrixDeterminant([[1,2],[2,4]]), 0)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
