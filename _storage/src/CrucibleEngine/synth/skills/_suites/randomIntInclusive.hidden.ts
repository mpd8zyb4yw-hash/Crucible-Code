// Generated by generate.ts — hidden adversarial suite for random-int-inclusive.
// Run via `npx tsx __audit__/randomIntInclusive.hidden.ts` inside the scratch project.
import { randomIntInclusive } from '../src/randomIntInclusive'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("min when rng=0", randomIntInclusive(5,10,()=>0), 5)
await check("max when rng near 1", randomIntInclusive(5,10,()=>0.9999), 10)
await check("mid range", randomIntInclusive(0,9,()=>0.5), 5)
await check("single value", randomIntInclusive(7,7,()=>0), 7)
await check("is integer", Number.isInteger(randomIntInclusive(1,100,()=>0.4)), true)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
