// Generated by generate.ts — hidden adversarial suite for rotate-point-degrees.
// Run via `npx tsx __audit__/rotatePointDeg.hidden.ts` inside the scratch project.
import { rotatePointDeg } from '../src/rotatePointDeg'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("90 deg around origin", rotatePointDeg(1,0,0,0,90), [0,1])
await check("180 deg around origin", rotatePointDeg(1,0,0,0,180), [-1,0])
await check("0 deg is identity", rotatePointDeg(5,3,0,0,0), [5,3])
await check("90 deg around non-origin center", rotatePointDeg(2,0,1,0,90), [1,1])
await check("full 360 returns to start", rotatePointDeg(3,4,0,0,360), [3,4])
await check("45 deg rounded to 6 decimals", rotatePointDeg(1,0,0,0,45), [0.707107,0.707107])
await check("270 deg around origin", rotatePointDeg(1,0,0,0,270), [0,-1])
await check("center point unchanged", rotatePointDeg(2,2,2,2,123), [2,2])
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
