// Generated by generate.ts — hidden adversarial suite for humanize-list.
// Run via `npx tsx __audit__/humanizeList.hidden.ts` inside the scratch project.
import { humanizeList } from '../src/humanizeList'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("empty", humanizeList([]), "")
await check("one", humanizeList(["a"]), "a")
await check("two", humanizeList(["a","b"]), "a and b")
await check("three", humanizeList(["a","b","c"]), "a, b and c")
await check("four", humanizeList(["a","b","c","d"]), "a, b, c and d")
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
