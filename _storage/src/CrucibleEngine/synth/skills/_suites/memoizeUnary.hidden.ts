// Generated by generate.ts — hidden adversarial suite for memoize-unary.
// Run via `npx tsx __audit__/memoizeUnary.hidden.ts` inside the scratch project.
import { memoizeUnary } from '../src/memoizeUnary'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("correct value", memoizeUnary((x:number)=>x*2)(5), 10)
await check("caches", (() => { let n=0; const f=memoizeUnary((x:number)=>{n++;return x}); f(1); f(1); f(1); return n })(), 1)
await check("distinct args", (() => { let n=0; const f=memoizeUnary((x:number)=>{n++;return x}); f(1); f(2); f(1); return n })(), 2)
await check("returns cached", (() => { let n=0; const f=memoizeUnary((x:number)=>++n); return [f(7),f(7)] })(), [1,1])
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
