// Generated by generate.ts — hidden adversarial suite for camel-case.
// Run via `npx tsx __audit__/camelCase.hidden.ts` inside the scratch project.
import { camelCase } from '../src/camelCase'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("kebab to camel", camelCase("hello-world"), "helloWorld")
await check("snake to camel", camelCase("hello_world"), "helloWorld")
await check("space to camel", camelCase("hello world"), "helloWorld")
await check("already camel", camelCase("helloWorld"), "helloWorld")
await check("PascalCase input", camelCase("HelloWorld"), "helloWorld")
await check("multiple words", camelCase("the-quick-brown-fox"), "theQuickBrownFox")
await check("empty", camelCase(""), "")
await check("single word", camelCase("hello"), "hello")
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
