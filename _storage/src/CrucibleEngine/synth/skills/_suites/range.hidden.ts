// Generated by generate.ts — hidden adversarial suite for range.
// Run via `npx tsx __audit__/range.hidden.ts` inside the scratch project.
import { range } from '../src/range'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("range(5)", range(5), [0,1,2,3,4])
await check("range(1,5)", range(1,5), [1,2,3,4])
await check("range with step", range(0,10,2), [0,2,4,6,8])
await check("range(0,0)", range(0,0), [])
await check("range descending", range(5,0,-1), [5,4,3,2,1])
await check("range(3,3)", range(3,3), [])
await check("range(0,1)", range(0,1), [0])
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
