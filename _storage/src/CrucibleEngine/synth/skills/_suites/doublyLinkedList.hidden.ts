// Generated by generate.ts — hidden adversarial suite for doubly-linked-list.
// Run via `npx tsx __audit__/doublyLinkedList.hidden.ts` inside the scratch project.
import { DoublyLinkedList } from '../src/doublyLinkedList'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("push and toArray", (() => { const l = new DoublyLinkedList<number>(); [1,2,3].forEach(n=>l.push(n)); return l.toArray() })(), [1,2,3])
await check("pop", (() => { const l = new DoublyLinkedList<number>(); l.push(1); l.push(2); return l.pop() })(), 2)
await check("unshift", (() => { const l = new DoublyLinkedList<number>(); l.push(2); l.unshift(1); return l.toArray() })(), [1,2])
await check("shift", (() => { const l = new DoublyLinkedList<number>(); [1,2,3].forEach(n=>l.push(n)); return l.shift() })(), 1)
await check("toArrayReverse", (() => { const l = new DoublyLinkedList<number>(); [1,2,3].forEach(n=>l.push(n)); return l.toArrayReverse() })(), [3,2,1])
await check("size", (() => { const l = new DoublyLinkedList<number>(); l.push(1); l.push(2); return l.size() })(), 2)
await check("isEmpty", new DoublyLinkedList().isEmpty(), true)
await check("pop empty", new DoublyLinkedList().pop(), undefined)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
