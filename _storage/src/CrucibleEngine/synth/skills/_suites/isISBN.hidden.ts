// Generated by generate.ts — hidden adversarial suite for is-isbn.
// Run via `npx tsx __audit__/isISBN.hidden.ts` inside the scratch project.
import { isISBN } from '../src/isISBN'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("valid ISBN-10", isISBN("0306406152"), true)
await check("valid ISBN-10 with hyphens", isISBN("0-306-40615-2"), true)
await check("valid ISBN-10 ending in X check digit", isISBN("097522980X"), true)
await check("valid ISBN-13", isISBN("9780306406157"), true)
await check("another valid ISBN-13", isISBN("9783161484100"), true)
await check("invalid 10-digit checksum", isISBN("1234567890"), false)
await check("empty string invalid", isISBN(""), false)
await check("12 digits is neither length", isISBN("978030640615"), false)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
