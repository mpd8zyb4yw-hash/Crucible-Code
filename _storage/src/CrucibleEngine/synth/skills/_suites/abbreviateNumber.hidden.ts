// Generated by generate.ts — hidden adversarial suite for abbreviate-number.
// Run via `npx tsx __audit__/abbreviateNumber.hidden.ts` inside the scratch project.
import { abbreviateNumber } from '../src/abbreviateNumber'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("1200", abbreviateNumber(1200), "1.2K")
await check("1500000", abbreviateNumber(1500000), "1.5M")
await check("1000000000", abbreviateNumber(1000000000), "1B")
await check("small", abbreviateNumber(999), "999")
await check("exact 1K", abbreviateNumber(1000), "1K")
await check("negative", abbreviateNumber(-2000), "-2K")
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
