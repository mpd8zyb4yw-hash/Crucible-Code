// Generated by generate.ts — hidden adversarial suite for date-range-days.
// Run via `npx tsx __audit__/dateRangeDays.hidden.ts` inside the scratch project.
import { dateRangeDays } from '../src/dateRangeDays'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("three days", dateRangeDays(new Date("2024-01-01T00:00:00Z"),new Date("2024-01-03T00:00:00Z")), ["2024-01-01","2024-01-02","2024-01-03"])
await check("same day", dateRangeDays(new Date("2024-06-15T00:00:00Z"),new Date("2024-06-15T00:00:00Z")), ["2024-06-15"])
await check("length", dateRangeDays(new Date("2024-01-01T00:00:00Z"),new Date("2024-01-07T00:00:00Z")).length, 7)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
