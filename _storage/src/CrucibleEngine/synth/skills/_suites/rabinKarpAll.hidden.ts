// Generated by generate.ts — hidden adversarial suite for rabin-karp-all.
// Run via `npx tsx __audit__/rabinKarpAll.hidden.ts` inside the scratch project.
import { rabinKarpAll } from '../src/rabinKarpAll'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("overlaps", rabinKarpAll("aaaa","aa"), [0,1,2])
await check("two matches", rabinKarpAll("abcabc","abc"), [0,3])
await check("no match", rabinKarpAll("abc","d"), [])
await check("single char", rabinKarpAll("aaa","a"), [0,1,2])
await check("empty text", rabinKarpAll("","a"), [])
await check("empty pattern", rabinKarpAll("abc",""), [])
await check("pattern longer", rabinKarpAll("ab","abc"), [])
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
