// Generated by generate.ts — hidden adversarial suite for circular-buffer.
// Run via `npx tsx __audit__/circularBuffer.hidden.ts` inside the scratch project.
import { CircularBuffer } from '../src/circularBuffer'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("within cap", (() => { const b = new CircularBuffer<number>(3); b.push(1); b.push(2); return b.toArray() })(), [1,2])
await check("overwrite oldest", (() => { const b = new CircularBuffer<number>(3); [1,2,3,4].forEach(n=>b.push(n)); return b.toArray() })(), [2,3,4])
await check("overwrite two", (() => { const b = new CircularBuffer<number>(2); [1,2,3,4,5].forEach(n=>b.push(n)); return b.toArray() })(), [4,5])
await check("isFull true", (() => { const b = new CircularBuffer<number>(2); b.push(1); b.push(2); return b.isFull() })(), true)
await check("isFull false", (() => { const b = new CircularBuffer<number>(2); b.push(1); return b.isFull() })(), false)
await check("size capped", (() => { const b = new CircularBuffer<number>(2); [1,2,3].forEach(n=>b.push(n)); return b.size() })(), 2)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
