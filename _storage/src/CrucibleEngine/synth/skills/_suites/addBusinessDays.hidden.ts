// Generated by generate.ts — hidden adversarial suite for add-business-days.
// Run via `npx tsx __audit__/addBusinessDays.hidden.ts` inside the scratch project.
import { addBusinessDays } from '../src/addBusinessDays'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("add 5 from Mon", addBusinessDays(new Date("2024-01-01T00:00:00Z"),5).toISOString().slice(0,10), "2024-01-08")
await check("add 1 before weekend", addBusinessDays(new Date("2024-01-05T00:00:00Z"),1).toISOString().slice(0,10), "2024-01-08")
await check("add 0", addBusinessDays(new Date("2024-01-01T00:00:00Z"),0).toISOString().slice(0,10), "2024-01-01")
await check("cross weekend", addBusinessDays(new Date("2024-01-04T00:00:00Z"),2).toISOString().slice(0,10), "2024-01-08")
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
