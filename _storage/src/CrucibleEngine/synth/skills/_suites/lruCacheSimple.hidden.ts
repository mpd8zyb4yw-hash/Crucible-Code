// Generated by generate.ts — hidden adversarial suite for lru-cache-simple.
// Run via `npx tsx __audit__/lruCacheSimple.hidden.ts` inside the scratch project.
import { LRUCache } from '../src/lruCacheSimple'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("get after set", (() => { const c = new LRUCache<string,number>(3); c.set("a",1); return c.get("a") })(), 1)
await check("get missing", new LRUCache(3).get("x"), undefined)
await check("evicts LRU on overflow", (() => { const c = new LRUCache<string,number>(2); c.set("a",1); c.set("b",2); c.set("c",3); return c.has("a") })(), false)
await check("get refreshes recency", (() => { const c = new LRUCache<string,number>(2); c.set("a",1); c.set("b",2); c.get("a"); c.set("c",3); return c.has("a") })(), true)
await check("size", (() => { const c = new LRUCache(5); c.set("x",1); c.set("y",2); return c.size() })(), 2)
await check("clear", (() => { const c = new LRUCache(5); c.set("x",1); c.clear(); return c.size() })(), 0)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
