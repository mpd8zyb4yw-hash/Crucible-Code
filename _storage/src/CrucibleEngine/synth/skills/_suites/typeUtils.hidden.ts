// Generated by generate.ts — hidden adversarial suite for type-utils.
// Run via `npx tsx __audit__/typeUtils.hidden.ts` inside the scratch project.
import { isError, tryCatch, ok, err } from '../src/typeUtils'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("ok result", ok(42), {"ok":true,"value":42})
await check("err result", JSON.stringify(err("oops")), "{\"ok\":false,\"error\":\"oops\"}")
await check("tryCatch success", tryCatch(()=>42), {"ok":true,"value":42})
await check("tryCatch failure ok false", tryCatch(()=>{throw new Error("boom")}).ok, false)
await check("tryCatch error message", (tryCatch(()=>{throw new Error("boom")}) as any).error.message, "boom")
await check("isError true", isError(new Error("x")), true)
await check("isError false", isError("x"), false)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
