// Generated by generate.ts — hidden adversarial suite for compose-pipe.
// Run via `npx tsx __audit__/composePipe.hidden.ts` inside the scratch project.
import { composeFns, pipeFns } from '../src/composePipe'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("compose right to left", composeFns((x:number)=>x*2,(x:number)=>x+1)(3), 8)
await check("pipe left to right", pipeFns((x:number)=>x+1,(x:number)=>x*2)(3), 8)
await check("compose empty identity", composeFns()(42), 42)
await check("pipe empty identity", pipeFns()(42), 42)
await check("pipe three", pipeFns((x:number)=>x+1,(x:number)=>x*2,(x:number)=>x-3)(0), -1)
await check("compose single", composeFns((x:number)=>x*5)(4), 20)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
