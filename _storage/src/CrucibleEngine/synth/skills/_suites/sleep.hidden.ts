// Generated by generate.ts — hidden adversarial suite for sleep.
// Run via `npx tsx __audit__/sleep.hidden.ts` inside the scratch project.
import { sleep } from '../src/sleep'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("resolves", sleep(0), undefined)
await check("returns promise", sleep(0) instanceof Promise, true)
await check("awaitable", (async () => { await sleep(0); return true })(), true)
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
