// Generated by generate.ts — hidden adversarial suite for run-length-coding.
// Run via `npx tsx __audit__/runLengthCoding.hidden.ts` inside the scratch project.
import { runLengthEncode, runLengthDecode } from '../src/runLengthCoding'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("encode basic", runLengthEncode("aaabb"), "3a2b")
await check("encode empty", runLengthEncode(""), "")
await check("encode singles", runLengthEncode("abc"), "1a1b1c")
await check("decode basic", runLengthDecode("3a2b"), "aaabb")
await check("decode multidigit", runLengthDecode("12a").length, 12)
await check("roundtrip", runLengthDecode(runLengthEncode("aaabbbc")), "aaabbbc")
await check("encode run", runLengthEncode("aaa"), "3a")
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
