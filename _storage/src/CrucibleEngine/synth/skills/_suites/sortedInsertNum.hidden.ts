// Generated by generate.ts — hidden adversarial suite for sorted-insert.
// Run via `npx tsx __audit__/sortedInsertNum.hidden.ts` inside the scratch project.
import { sortedInsert } from '../src/sortedInsertNum'

let failures = 0
async function check(name: string, got: unknown, want: unknown) {
  got = await got  // resolve promises transparently
  const ok = JSON.stringify(got) === JSON.stringify(want)
  console.log(`  ${ok ? 'PASS' : 'FAIL'} — ${name}`)
  if (!ok) {
    console.log(`       got:  ${JSON.stringify(got)}`)
    console.log(`       want: ${JSON.stringify(want)}`)
    failures++
  }
}

;(async () => {
await check("middle", sortedInsert([1,3,5], 4), [1,3,4,5])
await check("front", sortedInsert([2,3,4], 1), [1,2,3,4])
await check("back", sortedInsert([1,2,3], 9), [1,2,3,9])
await check("empty", sortedInsert([], 5), [5])
await check("duplicate", sortedInsert([1,2,2,3], 2), [1,2,2,2,3])
await check("no mutate", (() => { const a=[1,3]; sortedInsert(a,2); return a })(), [1,3])
console.log(failures === 0 ? 'ALL PASS' : `${failures} FAILURE(S)`)
process.exit(failures === 0 ? 0 : 1)
})()
