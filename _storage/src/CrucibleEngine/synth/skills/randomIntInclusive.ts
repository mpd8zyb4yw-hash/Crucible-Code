// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — randomIntInclusive returns a random integer in [min, max] using an injected rng function returning values in [0,1).\nexport function randomIntInclusive(min, max, rng) {\n  return Math.floor(rng() * (max - min + 1)) + min\n}\n"

registerSkill({
  id: "random-int-inclusive",
  summary: "randomIntInclusive returns a random integer in [min, max] using an injected rng function returning values in [0,1).",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\brandomIntInclusive\\b", 'i'))) sc += 0.9
  if (s.has(new RegExp("injected.*rng.*inclusive|inclusive.*injected.*rng", 'i'))) sc += 0.7
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/randomIntInclusive.ts", content: IMPL }]
  },
})
