// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — pearsonCorrelation computes the Pearson correlation coefficient between two equal-length numeric arrays.\nexport function pearsonCorrelation(x: number[], y: number[]): number {\n  const n = x.length;\n  let mx = 0, my = 0;\n  for (let i = 0; i < n; i++) { mx += x[i]; my += y[i]; }\n  mx /= n; my /= n;\n  let num = 0, dx = 0, dy = 0;\n  for (let i = 0; i < n; i++) {\n    const a = x[i] - mx;\n    const b = y[i] - my;\n    num += a * b;\n    dx += a * a;\n    dy += b * b;\n  }\n  const den = Math.sqrt(dx * dy);\n  if (den === 0) return 0;\n  return num / den;\n}\n\n"

registerSkill({
  id: "pearson-correlation-coeff",
  summary: "pearsonCorrelation computes the Pearson correlation coefficient between two equal-length numeric arrays.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bpearsonCorrelation\\b", 'i'))) sc += 0.5
  if (s.has(new RegExp("Pearson correlation coefficient", 'i'))) sc += 0.2
  if (s.has(new RegExp("correlation", 'i'))) sc += 0.15
  if (s.has(new RegExp("numeric arrays", 'i'))) sc += 0.1
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/pearsonCorrelationCoeff.ts", content: IMPL }]
  },
})
