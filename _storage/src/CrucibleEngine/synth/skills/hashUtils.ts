// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — Fast non-cryptographic hash (djb2) and simple checksum utilities.\nexport function djb2Hash(str: string): number {\n  let hash = 5381\n  for (let i = 0; i < str.length; i++) {\n    hash = ((hash << 5) + hash) ^ str.charCodeAt(i)\n    hash |= 0  // convert to 32-bit int\n  }\n  return hash >>> 0  // unsigned\n}\n\nexport function simpleChecksum(str: string): number {\n  return str.split('').reduce((sum, c) => (sum + c.charCodeAt(0)) & 0xffff, 0)\n}\n"

registerSkill({
  id: "hash-utils",
  summary: "Fast non-cryptographic hash (djb2) and simple checksum utilities.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bdjb2\\b|\\bhash.*string|string.*hash", 'i'))) sc += 0.55
  if (s.has(new RegExp("\\bchecksum\\b", 'i'))) sc += 0.5
  if (s.has(new RegExp("\\bnon.*cryptograph.*hash|fast.*hash", 'i'))) sc += 0.4
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/hashUtils.ts", content: IMPL }]
  },
})
