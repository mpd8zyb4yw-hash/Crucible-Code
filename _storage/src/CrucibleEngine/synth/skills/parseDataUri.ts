// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — parseDataUri parses a data: URI into its MIME type, base64 flag, and data string, or returns null for malformed input.\nexport function parseDataUri(uri) {\n  const m = uri.match(/^data:([^;,]+)?(?:;(base64))?,(.*)$/)\n  if (!m) return null\n  return { mime: m[1] ?? 'text/plain', isBase64: m[2] === 'base64', data: m[3] }\n}\n"

registerSkill({
  id: "parse-data-uri",
  summary: "parseDataUri parses a data: URI into its MIME type, base64 flag, and data string, or returns null for malformed input.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bparseDataUri\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("data.*uri.*parse|parse.*data.*uri", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/parseDataUri.ts", content: IMPL }]
  },
})
