// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — FIFO queue with enqueue, dequeue, peek, size, isEmpty.\nexport class Queue<T> {\n  private items: T[] = []\n  private head = 0\n\n  enqueue(item: T): void { this.items.push(item) }\n  dequeue(): T | undefined {\n    if (this.head >= this.items.length) return undefined\n    const item = this.items[this.head++]\n    if (this.head > this.items.length / 2) { this.items = this.items.slice(this.head); this.head = 0 }\n    return item\n  }\n  peek(): T | undefined { return this.items[this.head] }\n  size(): number { return this.items.length - this.head }\n  isEmpty(): boolean { return this.size() === 0 }\n  toArray(): T[] { return this.items.slice(this.head) }\n  clear(): void { this.items = []; this.head = 0 }\n}\n"

registerSkill({
  id: "queue",
  summary: "FIFO queue with enqueue, dequeue, peek, size, isEmpty.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bqueue\\b.*(?:enqueue|dequeue|fifo)", 'i'))) sc += 0.65
  if (s.has(new RegExp("\\bfifo\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("\\bQueue\\b", 'i'))) sc += 0.4
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/queue.ts", content: IMPL }]
  },
})
