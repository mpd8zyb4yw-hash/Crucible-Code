// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — coinChangeWays counts the distinct combinations of coins that sum to an amount (order-independent).\nexport function coinChangeWays(coins: number[], amount: number): number {\n  const dp = new Array(amount + 1).fill(0)\n  dp[0] = 1\n  for (const c of coins)\n    for (let a = c; a <= amount; a++)\n      dp[a] += dp[a - c]\n  return dp[amount]\n}\n"

registerSkill({
  id: "coin-change-ways",
  summary: "coinChangeWays counts the distinct combinations of coins that sum to an amount (order-independent).",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bcoinChangeWays\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("coin change.*ways|number of combinations|count.*combinations", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/coinChangeWays.ts", content: IMPL }]
  },
})
