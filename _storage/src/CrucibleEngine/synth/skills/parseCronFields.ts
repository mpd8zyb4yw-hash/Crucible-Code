// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — parseCronFields splits a 5-field cron expression into named fields; returns null if not exactly 5 fields.\nexport function parseCronFields(expr) {\n  const parts = expr.trim().split(/\\s+/)\n  if (parts.length !== 5) return null\n  const [minute,hour,dayOfMonth,month,dayOfWeek] = parts\n  return { minute, hour, dayOfMonth, month, dayOfWeek }\n}\n"

registerSkill({
  id: "parse-cron-fields",
  summary: "parseCronFields splits a 5-field cron expression into named fields; returns null if not exactly 5 fields.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bparseCronFields\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("cron.*parse|parse.*cron.*expression", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/parseCronFields.ts", content: IMPL }]
  },
})
