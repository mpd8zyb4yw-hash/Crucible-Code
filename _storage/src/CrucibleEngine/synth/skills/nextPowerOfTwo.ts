// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — nextPowerOfTwo returns the smallest power of two greater than or equal to n (1 for n<=1).\nexport function nextPowerOfTwo(n: number): number {\n  if (n <= 1) return 1\n  let p = 1\n  while (p < n) p *= 2\n  return p\n}\n"

registerSkill({
  id: "next-power-of-two",
  summary: "nextPowerOfTwo returns the smallest power of two greater than or equal to n (1 for n<=1).",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bnextPowerOfTwo\\b", 'i'))) sc += 0.6
  if (s.has(new RegExp("power of two|next power", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/nextPowerOfTwo.ts", content: IMPL }]
  },
})
