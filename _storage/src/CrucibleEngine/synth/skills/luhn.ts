// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — Validate a credit card number using the Luhn algorithm.\nexport function luhn(num: string): boolean {\n  const digits = num.replace(/\\D/g, '')\n  if (!digits.length) return false\n  let sum = 0\n  let odd = true\n  for (let i = digits.length - 1; i >= 0; i--) {\n    let d = parseInt(digits[i], 10)\n    if (!odd) { d *= 2; if (d > 9) d -= 9 }\n    sum += d\n    odd = !odd\n  }\n  return sum % 10 === 0\n}\n"

registerSkill({
  id: "luhn",
  summary: "Validate a credit card number using the Luhn algorithm.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bluhn\\b", 'i'))) sc += 0.8
  if (s.has(new RegExp("\\bcredit.*card.*valid|card.*number.*valid", 'i'))) sc += 0.4
  if (s.has(new RegExp("\\bmod.*10|modulo.*10", 'i'))) sc += 0.3
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/luhn.ts", content: IMPL }]
  },
})
