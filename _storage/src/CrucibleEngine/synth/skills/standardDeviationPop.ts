// Generated by generate.ts from catalog.ts — edit catalog.ts, not this file.
import { registerSkill, type SpecFeatures, type SynthFile } from '../synthEngine'

const IMPL = "// Synthesized by Crucible — standardDeviation computes the population standard deviation of a numeric array, returning NaN for an empty input.\nexport function standardDeviation(arr: number[]): number {\n  const n = arr.length;\n  if (n === 0) return NaN;\n  let mean = 0;\n  for (const x of arr) mean += x;\n  mean /= n;\n  let sum = 0;\n  for (const x of arr) {\n    const d = x - mean;\n    sum += d * d;\n  }\n  return Math.sqrt(sum / n);\n}\n\n"

registerSkill({
  id: "standard-deviation-pop",
  summary: "standardDeviation computes the population standard deviation of a numeric array, returning NaN for an empty input.",
  match(s: SpecFeatures): number {
    let sc = 0
  if (s.has(new RegExp("\\bstandardDeviation\\b", 'i'))) sc += 0.5
  if (s.has(new RegExp("population standard deviation", 'i'))) sc += 0.2
  if (s.has(new RegExp("standard deviation", 'i'))) sc += 0.15
  if (s.has(new RegExp("numeric array", 'i'))) sc += 0.1
    return sc
  },
  emit(s: SpecFeatures): SynthFile[] {
    return [{ path: s.modulePath ?? "src/standardDeviationPop.ts", content: IMPL }]
  },
})
